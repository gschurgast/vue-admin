// Root-level Vite config that delegates to the PWA app config.
// This ensures running `vite` from the repository root uses the PWA's aliases and plugins.
import pwaConfig from './pwa/vite.config'

export default pwaConfig
