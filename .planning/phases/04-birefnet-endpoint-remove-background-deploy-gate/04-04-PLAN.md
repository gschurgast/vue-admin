---
phase: 04
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - api/src/Service/AssetTransformation/StepParams/RemoveBackgroundStepParams.php
  - api/src/Service/AssetTransformation/StepParams/StepParamsFactory.php
  - api/src/Service/AssetTransformation/StepHandler/RemoveBackgroundHandler.php
  - api/src/Service/AssetTransformation/TransformationLookup.php
  - api/config/services.yaml
  - api/tests/Unit/Service/AssetTransformation/StepHandler/RemoveBackgroundHandlerTest.php
  - api/tests/Unit/Service/AssetTransformation/StepParams/RemoveBackgroundStepParamsTest.php
  - api/tests/Unit/Service/AssetTransformation/StepParams/StepParamsFactoryTest.php
  - api/tests/Unit/Service/AssetTransformation/TransformationLookupTest.php
autonomous: true
requirements: [BGREMOVE-06]
user_setup: []

tags: [api, php, symfony, handler, dto, transformation-lookup, sync-gate-inversion]

must_haves:
  truths:
    - "DTO `RemoveBackgroundStepParams` readonly avec `model` (default `birefnet`) + `fallbackOnTimeout` (default false), validation Assert\\Choice sur model"
    - "`StepParamsFactory::fromStep()` route `StepType::REMOVE_BACKGROUND → RemoveBackgroundStepParams` (plus de UnsupportedStepTypeException)"
    - "`RemoveBackgroundHandler` étend `AbstractEmbedderStepHandler` ; endpoint `/img/remove-background` ; timeout 6000ms (D-18, 5s Python + 1s marge)"
    - "Auto-tag `app.step_handler` actif (héritage Interface) — `PipelineRunner` pickup automatique"
    - "`TransformationLookup::isAsyncStep()` ne retourne plus `true` pour REMOVE_BACKGROUND (REMOVE_BACKGROUND devient sync, traversable par `/t/*`)"
    - "`TransformationLookup::isAsyncStep()` retourne TOUJOURS `true` pour `ADD_BACKGROUND` avec `type=ai_prompt` (Phase 5 path async préservé)"
    - "Param `transformations.embedder_timeout_remove_background_ms` (default 6000) câblé dans services.yaml avec env override `EMBEDDER_TIMEOUT_REMOVE_BACKGROUND_MS`"
    - "MockHttpClient confirme : POST multipart, Content-Type response respecté, RetryableHttpClient ne retry PAS 504 (déjà retry'é Python)"
  artifacts:
    - path: "api/src/Service/AssetTransformation/StepParams/RemoveBackgroundStepParams.php"
      provides: "DTO readonly model + fallbackOnTimeout"
      contains: "final readonly class RemoveBackgroundStepParams"
    - path: "api/src/Service/AssetTransformation/StepHandler/RemoveBackgroundHandler.php"
      provides: "Handler étend AbstractEmbedderStepHandler"
      contains: "extends AbstractEmbedderStepHandler"
    - path: "api/config/services.yaml"
      provides: "Param embedder_timeout_remove_background_ms (6000ms)"
      contains: "embedder_timeout_remove_background_ms"
  key_links:
    - from: "api/src/Service/AssetTransformation/StepHandler/RemoveBackgroundHandler.php"
      to: "api/src/Service/AssetTransformation/StepHandler/AbstractEmbedderStepHandler.php"
      via: "extends + parent::__construct(\\$embedderClient, \\$defaultTimeoutMs)"
      pattern: "extends AbstractEmbedderStepHandler"
    - from: "api/src/Service/AssetTransformation/StepParams/StepParamsFactory.php"
      to: "api/src/Service/AssetTransformation/StepParams/RemoveBackgroundStepParams.php"
      via: "match StepType::REMOVE_BACKGROUND => RemoveBackgroundStepParams::class"
      pattern: "REMOVE_BACKGROUND.*=>.*RemoveBackgroundStepParams"
    - from: "api/src/Service/AssetTransformation/TransformationLookup.php"
      to: "api/src/Enum/StepType.php"
      via: "REMOVE_BACKGROUND retiré du branchement async"
      pattern: "isAsyncStep"
---

<objective>
Câbler côté Symfony l'orchestration sync du nouveau step `remove_background` : DTO Validator + Factory routing + Handler HTTP (extends Abstract) + paramètre timeout 6s + **inversion** du gate `TransformationLookup::isAsyncStep` (REMOVE_BACKGROUND quitte le périmètre async).

Purpose: Sans cela, le step persiste mais n'est pas servi via `/t/*` (404 hard de Phase 3). Avec cela, `/t/{code}/{id}.png` invoquant `remove_background` répond en sync < 8s via le PipelineRunner Phase 3.
Output: 4 fichiers PHP créés + 2 modifiés ; 6 stubs PHPUnit du Plan 04-01 virent au vert (3 handler + 3 DTO) ; tests `TransformationLookup` + `StepParamsFactory` étendus ; aucune régression Phase 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-CONTEXT.md
@.planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-RESEARCH.md
@.planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-VALIDATION.md
@.planning/phases/03-php-orchestrator-public-route-cache-lock-sync-only/03-01-SUMMARY.md
@.planning/phases/03-php-orchestrator-public-route-cache-lock-sync-only/03-02-SUMMARY.md
@api/src/Service/AssetTransformation/StepHandler/AbstractEmbedderStepHandler.php
@api/src/Service/AssetTransformation/StepHandler/ResizeStepHandler.php
@api/src/Service/AssetTransformation/StepParams/StepParamsFactory.php
@api/src/Service/AssetTransformation/TransformationLookup.php
@api/src/Enum/StepType.php
@api/config/services.yaml

<interfaces>
<!-- Contracts existants à respecter -->

From AbstractEmbedderStepHandler (Phase 3) :
- public function __construct(HttpClientInterface $embedderClient, int $defaultTimeoutMsValue)
- public function run(string $bytes, array $params, int $timeoutMs): HandlerResult
- abstract protected function endpointPath(): string
- public function defaultTimeoutMs(): int

From StepHandlerInterface (Phase 3) :
- public static function supportedType(): StepType
- #[AutoconfigureTag('app.step_handler')] — auto-tagging via attribute on interface

From StepType enum :
- StepType::REMOVE_BACKGROUND (case déjà présent depuis Phase 1)

From StepParamsFactory (Phase 3) :
- match expression with `default => throw new UnsupportedStepTypeException($type)`
- REMOVE_BACKGROUND tombe actuellement dans `default` — à câbler explicitement

From TransformationLookup (Phase 3) :
- private function isAsyncStep(TransformationStep $step): bool
  - actuellement retourne true pour REMOVE_BACKGROUND → 404 sur /t/*
  - DOIT être inversé : REMOVE_BACKGROUND devient sync, mais ADD_BACKGROUND ai_prompt reste async

Embedder endpoint (Plan 04-02) :
- POST /img/remove-background multipart `image` + form `params` JSON
- Headers réponse: Content-Type: image/png + X-Render-Duration-Ms + X-Model-Used

Existing test patterns (Phase 3) :
- HandlersHttpTest pattern : MockHttpClient + MockResponse, instantiate handler directly with mocks
- StepParamsFactoryTest pattern : direct instantiation with SerializerInterface + ValidatorInterface from container
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RemoveBackgroundStepParams DTO + StepParamsFactory routing + tests</name>
  <files>api/src/Service/AssetTransformation/StepParams/RemoveBackgroundStepParams.php, api/src/Service/AssetTransformation/StepParams/StepParamsFactory.php, api/tests/Unit/Service/AssetTransformation/StepParams/RemoveBackgroundStepParamsTest.php, api/tests/Unit/Service/AssetTransformation/StepParams/StepParamsFactoryTest.php</files>
  <read_first>
    - api/src/Service/AssetTransformation/StepParams/StepParamsFactory.php (Phase 3 — match expression à étendre)
    - api/src/Service/AssetTransformation/StepParams/AddBackgroundStepParams.php (référence DTO Phase 3 pour les patterns Assert)
    - api/tests/Unit/Service/AssetTransformation/StepParams/StepParamsFactoryTest.php (Phase 3 — pattern de tests existant)
    - .planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-RESEARCH.md (Pattern 5 verbatim)
  </read_first>
  <behavior>
    - DTO `RemoveBackgroundStepParams` :
      - `model: string = 'birefnet'` validé par `Assert\\Choice(['birefnet', 'isnet-general-use'])`
      - `fallbackOnTimeout: bool = false`
      - Strict-fields denormalisation enforced (toute clé hors `model`/`fallbackOnTimeout` → ExtraAttributesException → 422)
    - Factory : `match` ajoute case `StepType::REMOVE_BACKGROUND => RemoveBackgroundStepParams::class`
    - Tests DTO : 3 cas → defaults, model invalide rejeté, fallbackOnTimeout=false par défaut
    - Tests Factory : nouveau test `testRemoveBackgroundRouting` qui assert `fromStep(stepWithRemoveBackground)` retourne `RemoveBackgroundStepParams` — le test existant `testRemoveBackgroundThrowsUnsupportedStepType` (Phase 3) DOIT être supprimé ou inversé.
  </behavior>
  <action>
    1. **Créer `api/src/Service/AssetTransformation/StepParams/RemoveBackgroundStepParams.php`** :
    ```php
    <?php

    declare(strict_types=1);

    namespace App\Service\AssetTransformation\StepParams;

    use Symfony\Component\Validator\Constraints as Assert;

    /**
     * Plan 04 — DTO validator for the `remove_background` step (BGREMOVE-01..06).
     *
     * Contract: `POST /img/remove-background` (Phase 4 Python endpoint).
     * Only two params are accepted; any extra key (e.g. URL) is rejected at the
     * Doctrine listener stage as a denormalisation exception → 422.
     */
    final readonly class RemoveBackgroundStepParams
    {
        public function __construct(
            #[Assert\Choice(choices: ['birefnet', 'isnet-general-use'])]
            public string $model = 'birefnet',
            public bool $fallbackOnTimeout = false,
        ) {
        }
    }
    ```

    2. **Modifier `api/src/Service/AssetTransformation/StepParams/StepParamsFactory.php`** :
       - Dans la `match` expression, AJOUTER le case `REMOVE_BACKGROUND` :
       ```php
       $class = match ($type) {
           StepType::RESIZE         => ResizeStepParams::class,
           StepType::CROP           => CropStepParams::class,
           StepType::ROTATE         => RotateStepParams::class,
           StepType::FORMAT_CONVERT => FormatConvertStepParams::class,
           StepType::ADD_BACKGROUND => AddBackgroundStepParams::class,
           StepType::REMOVE_BACKGROUND => RemoveBackgroundStepParams::class,  // Phase 4
           default                  => throw new UnsupportedStepTypeException($type),
       };
       ```
       - Retirer le commentaire `// REMOVE_BACKGROUND wired in Phase 4 (BiRefNet).` qui devient obsolète.

    3. **Remplacer `api/tests/Unit/Service/AssetTransformation/StepParams/RemoveBackgroundStepParamsTest.php`** (qui n'a que `markTestSkipped` depuis Plan 04-01) :
    ```php
    <?php

    namespace App\Tests\Unit\Service\AssetTransformation\StepParams;

    use App\Service\AssetTransformation\StepParams\RemoveBackgroundStepParams;
    use PHPUnit\Framework\TestCase;
    use Symfony\Component\Validator\Validation;
    use Symfony\Component\Validator\Validator\ValidatorInterface;

    final class RemoveBackgroundStepParamsTest extends TestCase
    {
        private ValidatorInterface $validator;

        protected function setUp(): void
        {
            $this->validator = Validation::createValidatorBuilder()
                ->enableAttributeMapping()
                ->getValidator();
        }

        public function testDefaultModelIsBirefnet(): void
        {
            $dto = new RemoveBackgroundStepParams();
            self::assertSame('birefnet', $dto->model);
            self::assertFalse($dto->fallbackOnTimeout);
            self::assertCount(0, $this->validator->validate($dto));
        }

        public function testInvalidModelRejected(): void
        {
            $dto = new RemoveBackgroundStepParams(model: 'rmbg-1.4');
            $violations = $this->validator->validate($dto);
            self::assertGreaterThan(0, count($violations));
            self::assertStringContainsString('model', (string) $violations);
        }

        public function testFallbackOnTimeoutAcceptsTrue(): void
        {
            $dto = new RemoveBackgroundStepParams(model: 'birefnet', fallbackOnTimeout: true);
            self::assertTrue($dto->fallbackOnTimeout);
            self::assertCount(0, $this->validator->validate($dto));
        }

        public function testIsnetGeneralUseAccepted(): void
        {
            $dto = new RemoveBackgroundStepParams(model: 'isnet-general-use');
            self::assertCount(0, $this->validator->validate($dto));
        }
    }
    ```

    4. **Étendre `api/tests/Unit/Service/AssetTransformation/StepParams/StepParamsFactoryTest.php`** :
       - Localiser le test `testRemoveBackgroundThrowsUnsupportedStepType` (s'il existe — Phase 3). Le **supprimer ou inverser** : `REMOVE_BACKGROUND` n'est plus unsupported.
       - Ajouter un nouveau test `testRemoveBackgroundRouting` :
       ```php
       public function testRemoveBackgroundRouting(): void
       {
           $step = new TransformationStep();
           $step->setType(StepType::REMOVE_BACKGROUND);
           $step->setParams(['model' => 'birefnet', 'fallbackOnTimeout' => true]);

           $dto = $this->factory->fromStep($step);
           self::assertInstanceOf(RemoveBackgroundStepParams::class, $dto);
           self::assertSame('birefnet', $dto->model);
           self::assertTrue($dto->fallbackOnTimeout);
       }

       public function testRemoveBackgroundRejectsInvalidModel(): void
       {
           $step = new TransformationStep();
           $step->setType(StepType::REMOVE_BACKGROUND);
           $step->setParams(['model' => 'rmbg-1.4']);

           $this->expectException(\Symfony\Component\Validator\Exception\ValidationFailedException::class);
           $this->factory->fromStep($step);
       }

       public function testRemoveBackgroundRejectsUnknownKey(): void
       {
           $step = new TransformationStep();
           $step->setType(StepType::REMOVE_BACKGROUND);
           $step->setParams(['model' => 'birefnet', 'url' => 'http://evil.local']);   // SSRF vector

           $this->expectException(\Symfony\Component\Serializer\Exception\ExtraAttributesException::class);
           $this->factory->fromStep($step);
       }
       ```
       Note: le test existant `testUnsupportedStepTypeRaises` qui couvre déjà le `default => throw` doit être préservé en utilisant un cas autre que REMOVE_BACKGROUND (ou supprimé si REMOVE_BACKGROUND était le seul restant).
  </action>
  <verify>
    <automated>test -f api/src/Service/AssetTransformation/StepParams/RemoveBackgroundStepParams.php && grep -q "REMOVE_BACKGROUND.*=>.*RemoveBackgroundStepParams" api/src/Service/AssetTransformation/StepParams/StepParamsFactory.php && docker compose exec -T api vendor/bin/phpunit --filter "RemoveBackgroundStepParams|StepParamsFactory" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "final readonly class RemoveBackgroundStepParams" api/src/Service/AssetTransformation/StepParams/RemoveBackgroundStepParams.php`
    - `grep -q "Assert\\\\Choice(choices: \\['birefnet', 'isnet-general-use'\\])" api/src/Service/AssetTransformation/StepParams/RemoveBackgroundStepParams.php`
    - `grep -q "REMOVE_BACKGROUND => RemoveBackgroundStepParams::class" api/src/Service/AssetTransformation/StepParams/StepParamsFactory.php`
    - `docker compose exec -T api vendor/bin/phpunit --filter RemoveBackgroundStepParamsTest 2>&1 | grep -E "OK|FAILURES"` → OK (4 tests, no skip)
    - `docker compose exec -T api vendor/bin/phpunit --filter StepParamsFactoryTest 2>&1 | grep -E "OK|FAILURES"` → OK (tests existants + 3 nouveaux verts)
    - Aucune régression sur les autres DTO Phase 3
  </acceptance_criteria>
  <done>DTO + Factory câblés + 4 tests DTO verts + 3 nouveaux tests Factory verts (routing/invalid/SSRF).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RemoveBackgroundHandler + services.yaml param + tests MockHttpClient</name>
  <files>api/src/Service/AssetTransformation/StepHandler/RemoveBackgroundHandler.php, api/config/services.yaml, api/tests/Unit/Service/AssetTransformation/StepHandler/RemoveBackgroundHandlerTest.php</files>
  <read_first>
    - api/src/Service/AssetTransformation/StepHandler/ResizeStepHandler.php (référence verbatim — handler concret minimal)
    - api/src/Service/AssetTransformation/StepHandler/AbstractEmbedderStepHandler.php
    - api/config/services.yaml (lignes parameters: 19-28 — pattern des timeouts)
    - api/tests/Unit/Service/AssetTransformation/StepHandler/HandlersHttpTest.php (référence MockHttpClient Phase 3)
    - .planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-RESEARCH.md (Pattern 5 + D-18)
  </read_first>
  <behavior>
    - `RemoveBackgroundHandler extends AbstractEmbedderStepHandler`
    - `supportedType()` returns `StepType::REMOVE_BACKGROUND`
    - `endpointPath()` returns `/img/remove-background`
    - DI : `embedder.client` + param `transformations.embedder_timeout_remove_background_ms` (default 6000ms = 5s Python + 1s marge réseau)
    - Auto-tag `app.step_handler` via héritage de l'interface (vérifié Phase 3)
    - Param exposé en services.yaml avec env override `EMBEDDER_TIMEOUT_REMOVE_BACKGROUND_MS`
  </behavior>
  <action>
    1. **Créer `api/src/Service/AssetTransformation/StepHandler/RemoveBackgroundHandler.php`** (clone ResizeStepHandler) :
    ```php
    <?php

    namespace App\Service\AssetTransformation\StepHandler;

    use App\Enum\StepType;
    use Symfony\Component\DependencyInjection\Attribute\Autowire;
    use Symfony\Contracts\HttpClient\HttpClientInterface;

    /**
     * Plan 04 — BGREMOVE-06. Calls POST /img/remove-background on the
     * embedder service (Phase 4 Python endpoint, D-18).
     *
     * Timeout: 6000 ms = 5 s Python hard cap + 1 s network margin.
     * Retry: inherits embedder.client policy — 5xx + transport only; 504 is
     * NOT retried (already retried Python-side via fallback).
     */
    final class RemoveBackgroundHandler extends AbstractEmbedderStepHandler
    {
        public function __construct(
            #[Autowire(service: 'embedder.client')] HttpClientInterface $embedderClient,
            #[Autowire(param: 'transformations.embedder_timeout_remove_background_ms')] int $defaultTimeoutMs,
        ) {
            parent::__construct($embedderClient, $defaultTimeoutMs);
        }

        public static function supportedType(): StepType
        {
            return StepType::REMOVE_BACKGROUND;
        }

        protected function endpointPath(): string
        {
            return '/img/remove-background';
        }
    }
    ```

    2. **Modifier `api/config/services.yaml`** — AJOUTER dans la section `parameters:` (à la suite des autres `embedder_timeout_*`) :
    ```yaml
        embedder_default_rmbg_ms: '6000'   # D-18: 5s Python + 1s network margin
        transformations.embedder_timeout_remove_background_ms: '%env(int:default:embedder_default_rmbg_ms:EMBEDDER_TIMEOUT_REMOVE_BACKGROUND_MS)%'
    ```
    Aucune autre modification : le tag `app.step_handler` est implicite via `#[AutoconfigureTag]` sur l'interface (vérifié Phase 3 Plan 03-02 SUMMARY).

    3. **Remplacer `api/tests/Unit/Service/AssetTransformation/StepHandler/RemoveBackgroundHandlerTest.php`** (qui n'a que `markTestSkipped` depuis Plan 04-01) — pattern de HandlersHttpTest :
    ```php
    <?php

    namespace App\Tests\Unit\Service\AssetTransformation\StepHandler;

    use App\Enum\StepType;
    use App\Service\AssetTransformation\StepHandler\RemoveBackgroundHandler;
    use PHPUnit\Framework\TestCase;
    use Symfony\Component\HttpClient\MockHttpClient;
    use Symfony\Component\HttpClient\Response\MockResponse;

    final class RemoveBackgroundHandlerTest extends TestCase
    {
        public function testHandlerPostsToRemoveBackgroundEndpoint(): void
        {
            $requestedUrl = null;
            $client = new MockHttpClient(function (string $method, string $url) use (&$requestedUrl): MockResponse {
                $requestedUrl = "$method $url";
                return new MockResponse(
                    body: "PNG-bytes-here",
                    info: ['http_code' => 200, 'response_headers' => [
                        'content-type: image/png',
                        'x-render-duration-ms: 1850',
                        'x-model-used: birefnet',
                    ]],
                );
            }, 'http://embedder:8000');

            $handler = new RemoveBackgroundHandler($client, 6000);
            $result = $handler->run('FAKE-INPUT', ['model' => 'birefnet'], 6000);

            self::assertSame('PNG-bytes-here', $result->bytes);
            self::assertSame('image/png', $result->contentType);
            self::assertSame(1850, $result->renderMs);
            self::assertStringContainsString('POST http://embedder:8000/img/remove-background', $requestedUrl);
        }

        public function testSupportedTypeIsRemoveBackground(): void
        {
            self::assertSame(StepType::REMOVE_BACKGROUND, RemoveBackgroundHandler::supportedType());
        }

        public function testDefaultTimeoutMsReflectsConstructorInjection(): void
        {
            $client = new MockHttpClient([], 'http://embedder:8000');
            $handler = new RemoveBackgroundHandler($client, 6000);
            self::assertSame(6000, $handler->defaultTimeoutMs());
        }

        public function testHandlerDoesNotRetryOn504(): void
        {
            // The retry strategy is configured on embedder.client (services.yaml).
            // In this unit test we use a bare MockHttpClient → no retry happens
            // anyway, but we assert that 504 propagates as a TransformationPipelineException
            // (not silently swallowed).
            $callCount = 0;
            $client = new MockHttpClient(function () use (&$callCount): MockResponse {
                $callCount++;
                return new MockResponse(body: 'timeout', info: ['http_code' => 504, 'response_headers' => ['content-type: application/json']]);
            }, 'http://embedder:8000');

            $handler = new RemoveBackgroundHandler($client, 6000);

            $this->expectException(\App\Service\AssetTransformation\TransformationPipelineException::class);
            try {
                $handler->run('FAKE-INPUT', ['model' => 'birefnet'], 6000);
            } finally {
                // Without retry strategy in MockHttpClient → exactly 1 call.
                self::assertSame(1, $callCount, 'No retry should happen on 504 at handler unit level');
            }
        }
    }
    ```

    4. Vérifier que le handler est bien picked up par le PipelineRunner via :
       ```
       docker compose exec api php bin/console debug:container --tag=app.step_handler
       ```
       doit lister 6 handlers (Resize, Crop, Rotate, FormatConvert, AddBackground, **RemoveBackground**).
  </action>
  <verify>
    <automated>test -f api/src/Service/AssetTransformation/StepHandler/RemoveBackgroundHandler.php && grep -q "extends AbstractEmbedderStepHandler" api/src/Service/AssetTransformation/StepHandler/RemoveBackgroundHandler.php && grep -q "/img/remove-background" api/src/Service/AssetTransformation/StepHandler/RemoveBackgroundHandler.php && grep -q "embedder_default_rmbg_ms: '6000'" api/config/services.yaml && grep -q "transformations.embedder_timeout_remove_background_ms" api/config/services.yaml && docker compose exec -T api vendor/bin/phpunit --filter RemoveBackgroundHandlerTest 2>&1 | tail -10 && docker compose exec -T api php bin/console debug:container --tag=app.step_handler 2>&1 | grep -i remove</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "final class RemoveBackgroundHandler extends AbstractEmbedderStepHandler" api/src/Service/AssetTransformation/StepHandler/RemoveBackgroundHandler.php`
    - `grep -q "return StepType::REMOVE_BACKGROUND" api/src/Service/AssetTransformation/StepHandler/RemoveBackgroundHandler.php`
    - `grep -q "return '/img/remove-background'" api/src/Service/AssetTransformation/StepHandler/RemoveBackgroundHandler.php`
    - `grep -q "embedder_default_rmbg_ms: '6000'" api/config/services.yaml`
    - `grep -q "EMBEDDER_TIMEOUT_REMOVE_BACKGROUND_MS" api/config/services.yaml`
    - `docker compose exec -T api vendor/bin/phpunit --filter RemoveBackgroundHandlerTest` → 4 tests OK, no skip
    - `docker compose exec -T api php bin/console debug:container --tag=app.step_handler` → 6 handlers listés dont RemoveBackgroundHandler
    - Aucune régression sur HandlersHttpTest (Phase 3) : `vendor/bin/phpunit --testsuite=unit` reste vert
  </acceptance_criteria>
  <done>Handler créé + param timeout 6s câblé + 4 tests MockHttpClient verts + handler picked up par PipelineRunner via tag DI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Inverser TransformationLookup::isAsyncStep pour REMOVE_BACKGROUND + tests</name>
  <files>api/src/Service/AssetTransformation/TransformationLookup.php, api/tests/Unit/Service/AssetTransformation/TransformationLookupTest.php</files>
  <read_first>
    - api/src/Service/AssetTransformation/TransformationLookup.php (méthode `isAsyncStep` lignes 61-72 — branche REMOVE_BACKGROUND à RETIRER)
    - api/tests/Unit/Service/AssetTransformation/TransformationLookupTest.php (s'il existe — sinon créer)
    - api/tests/Integration/Controller/PublicTransformationControllerTest.php (Phase 3 — chercher tests qui asserent 404 sur REMOVE_BACKGROUND, à mettre à jour)
    - .planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-RESEARCH.md (Pattern 6 verbatim + Open Question 3)
  </read_first>
  <behavior>
    - `isAsyncStep(REMOVE_BACKGROUND step)` retourne désormais `false` (était `true` Phase 3)
    - `isAsyncStep(ADD_BACKGROUND step with type:ai_prompt)` retourne TOUJOURS `true` (préserver path async Phase 5)
    - `isAsyncStep(ADD_BACKGROUND step with type:color)` retourne `false` (sync)
    - `findOr404` ne retourne plus 404 pour une transformation contenant REMOVE_BACKGROUND seul
    - `findOr404` retourne TOUJOURS 404 pour une transformation contenant ADD_BACKGROUND ai_prompt (Phase 5)
    - Tests Phase 3 qui asserent 404 sur REMOVE_BACKGROUND sont MIS À JOUR (pas supprimés — inversés)
  </behavior>
  <action>
    1. **Avant toute modification**, exécuter :
    ```bash
    docker compose exec -T api grep -rn "REMOVE_BACKGROUND" api/tests/ api/src/Service/AssetTransformation/ 2>&1
    ```
    Lister les call-sites pour identifier les tests à mettre à jour (notamment dans `PublicTransformationControllerTest`, `PipelineRunnerTest`, `StepParamsFactoryTest`).

    2. **Modifier `api/src/Service/AssetTransformation/TransformationLookup.php`** :
       - Retirer le block `if ($type === StepType::REMOVE_BACKGROUND) { return true; }` (lignes 63-65)
       - Mettre à jour le PHPDoc en tête de classe : remplacer la mention "REMOVE_BACKGROUND or ADD_BACKGROUND with type=ai_prompt" par "ADD_BACKGROUND with type=ai_prompt only"
       - Mettre à jour le commentaire `D-05 — sync-only AI gating` :
       ```php
       // D-05 + Phase 4 D-16 — sync-only AI gating. Phase 4 introduces a sync
       // remove_background handler (BiRefNet/isnet, < 8s wall-clock); only the
       // SD-backed `add_background type:ai_prompt` still requires the async path.
       ```
       Résultat final de `isAsyncStep` :
       ```php
       private function isAsyncStep(TransformationStep $step): bool
       {
           $type = $step->getType();
           // Phase 4 (D-16): REMOVE_BACKGROUND is now served sync via Plan 04-02/04-04.
           if ($type === StepType::ADD_BACKGROUND) {
               $params = $step->getParams();
               return ($params['type'] ?? null) === 'ai_prompt';
           }
           return false;
       }
       ```

    3. **Créer ou étendre `api/tests/Unit/Service/AssetTransformation/TransformationLookupTest.php`** avec ces 4 tests :
    ```php
    <?php

    namespace App\Tests\Unit\Service\AssetTransformation;

    use App\Entity\Asset\Asset;
    use App\Entity\AssetTransformation\AssetTransformation;
    use App\Entity\AssetTransformation\TransformationStep;
    use App\Enum\StepType;
    use App\Service\AssetTransformation\TransformationLookup;
    use Doctrine\ORM\EntityManagerInterface;
    use Doctrine\ORM\EntityRepository;
    use PHPUnit\Framework\TestCase;
    use Symfony\Component\HttpKernel\Exception\NotFoundHttpException;

    final class TransformationLookupTest extends TestCase
    {
        private function makeLookup(?AssetTransformation $tx, ?Asset $asset): TransformationLookup
        {
            $em = $this->createMock(EntityManagerInterface::class);
            $repo = $this->createMock(EntityRepository::class);
            $repo->method('findOneBy')->willReturn($tx);
            $em->method('getRepository')->willReturn($repo);
            $em->method('find')->willReturn($asset);
            return new TransformationLookup($em);
        }

        private function makeTx(StepType $type, array $params = []): AssetTransformation
        {
            $tx = new AssetTransformation();
            $tx->setCode('foo');
            $step = new TransformationStep();
            $step->setType($type);
            $step->setParams($params);
            $tx->addStep($step);
            return $tx;
        }

        public function testRemoveBackgroundSyncIsServed(): void
        {
            // Phase 4 D-16: REMOVE_BACKGROUND no longer 404s.
            $tx = $this->makeTx(StepType::REMOVE_BACKGROUND, ['model' => 'birefnet']);
            $asset = new Asset();
            $asset->setIsPublic(true);
            $lookup = $this->makeLookup($tx, $asset);

            [$resolvedTx, $resolvedAsset] = $lookup->findOr404('foo', 42);
            self::assertSame($tx, $resolvedTx);
            self::assertSame($asset, $resolvedAsset);
        }

        public function testAddBackgroundAiPromptStill404(): void
        {
            // Phase 5 path: ai_prompt remains async, must 404 in Phase 4.
            $tx = $this->makeTx(StepType::ADD_BACKGROUND, ['type' => 'ai_prompt', 'prompt' => 'studio']);
            $lookup = $this->makeLookup($tx, null);

            $this->expectException(NotFoundHttpException::class);
            $lookup->findOr404('foo', 42);
        }

        public function testAddBackgroundColorIsServed(): void
        {
            // color path is sync — must NOT 404.
            $tx = $this->makeTx(StepType::ADD_BACKGROUND, ['type' => 'color', 'color' => '#FFFFFF']);
            $asset = new Asset();
            $asset->setIsPublic(true);
            $lookup = $this->makeLookup($tx, $asset);

            [$resolvedTx, ] = $lookup->findOr404('foo', 42);
            self::assertSame($tx, $resolvedTx);
        }

        public function testNonPublicAssetStill404OnRemoveBackground(): void
        {
            // ROUTE-08 unchanged: isPublic=false → 404 even with sync steps.
            $tx = $this->makeTx(StepType::REMOVE_BACKGROUND, ['model' => 'birefnet']);
            $asset = new Asset();
            $asset->setIsPublic(false);
            $lookup = $this->makeLookup($tx, $asset);

            $this->expectException(NotFoundHttpException::class);
            $lookup->findOr404('foo', 42);
        }
    }
    ```

    4. **Mettre à jour les tests d'intégration Phase 3 qui asserent 404 sur REMOVE_BACKGROUND** (s'ils existent — découverts par le grep step 1) :
       - Dans `api/tests/Integration/Controller/PublicTransformationControllerTest.php` : si un test type `testRemoveBackgroundReturns404` existe, le renommer/inverser en `testRemoveBackgroundIsServedSyncAfterPhase4` ou similaire. À ce niveau d'intégration, le handler appellera réellement embedder.client — utiliser MockHttpClient global ou un attribut `@group integration_ml` si besoin de skip en CI sans modèles.
       - Si le test précis n'existe pas en Phase 3, ne pas en créer en Phase 4 : la couverture sync est déjà assurée par `PipelineRunnerTest` Phase 3 + le nouveau `TransformationLookupTest` Plan 04-04.

    5. **Tester aussi `PipelineRunnerTest::testUnsupportedStepTypeRaises`** (Phase 3) : ce test utilisait `REMOVE_BACKGROUND` comme exemple unsupported (vu dans le SUMMARY Plan 03-02). Maintenant que REMOVE_BACKGROUND est supporté, ce test casse. Le **mettre à jour** pour utiliser un StepType réellement non géré OU le supprimer si plus pertinent :
       - Option A (recommandée) : créer dans le test un step avec `setType(null)` ou un type fictif via reflection → ce qui force l'erreur LogicException
       - Option B : supprimer le test (acceptable car REMOVE_BACKGROUND était le seul case unsupported, et la couverture est désormais assurée par les tests DTO Factory Task 1)
       - Choisir l'Option B si le test devient artificiel, en notant le retrait dans le commit message.
  </action>
  <verify>
    <automated>grep -A 8 "private function isAsyncStep" api/src/Service/AssetTransformation/TransformationLookup.php | head -12 && ! grep -A 2 "isAsyncStep" api/src/Service/AssetTransformation/TransformationLookup.php | grep -q "REMOVE_BACKGROUND" && docker compose exec -T api vendor/bin/phpunit --filter "TransformationLookupTest" 2>&1 | tail -10 && docker compose exec -T api vendor/bin/phpunit --testsuite=unit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 10 "private function isAsyncStep" api/src/Service/AssetTransformation/TransformationLookup.php | grep -c "REMOVE_BACKGROUND"` = 0 (plus de référence à REMOVE_BACKGROUND dans la méthode)
    - `grep -A 10 "private function isAsyncStep" api/src/Service/AssetTransformation/TransformationLookup.php | grep -q "ai_prompt"` (ADD_BACKGROUND ai_prompt préservé)
    - `docker compose exec -T api vendor/bin/phpunit --filter TransformationLookupTest 2>&1 | grep -E "OK|FAILURES"` → OK (4 tests)
    - `docker compose exec -T api vendor/bin/phpunit --testsuite=unit 2>&1 | grep -E "OK|FAILURES"` → OK (régression Phase 3 zéro)
    - `docker compose exec -T api vendor/bin/phpunit --testsuite=integration 2>&1 | grep -E "OK|FAILURES"` → OK
  </acceptance_criteria>
  <done>isAsyncStep n'inclut plus REMOVE_BACKGROUND, ai_prompt préservé pour Phase 5, 4 tests TransformationLookupTest verts, tests Phase 3 impactés mis à jour, suite unit+integration verte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 04-04)

| Boundary | Description |
|----------|-------------|
| API Platform write (admin user) → DB | Step params hydratés via StepParamsFactory + validation Doctrine listener Phase 3 — toute clé inconnue → 422 |
| PHP handler → embedder service | Scoping `http://embedder:8000` (Phase 3 D-06) + RetryableHttpClient (200/400/800ms, 5xx + transport) |
| Route publique `/t/*` → utilisateur final | Désormais accessible pour des transformations contenant `remove_background` — surface élargie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | SSRF | params `model` ou ajout d'une clé `url` malicieuse | mitigate | Pydantic enum côté Python + `Assert\\Choice` côté PHP + denormalisation strict-fields refuse toute clé hors enum (testé par `testRemoveBackgroundRejectsUnknownKey`) |
| T-04-05 | Info disclosure | Inversion gate `isAsyncStep` → la route 404 unifiée de Phase 3 perdrait son comportement uniforme | mitigate | TransformationLookup conserve l'invariant T-03-11 (toute rejection → 404, jamais 403) ; testé par `testNonPublicAssetStill404OnRemoveBackground` |
| T-04-06 | DoS amplification | Retry sur 504 (déjà géré par fallback Python) → doubling | mitigate | RetryableHttpClient existant exclut explicitement les 4xx ; 504 = 504 (5xx) DONC retry — clarifier via `testHandlerDoesNotRetryOn504` au niveau unit. Au niveau integration, le retry est compté mais le total handler-side reste ≤ 4 appels (1 initial + 3 retries) avec backoff 200/400/800 = 1.4s ; encore inscrit dans le cap 8s. ACCEPTÉ (la fallback Python permet déjà à un seul appel HTTP de tenter 2 modèles). |
</threat_model>

<verification>
- `docker compose exec -T api vendor/bin/phpunit --testsuite=unit` → 0 failed
- `docker compose exec -T api vendor/bin/phpunit --testsuite=integration` → 0 failed
- `docker compose exec -T api php bin/console debug:container --tag=app.step_handler` → 6 handlers (incl. RemoveBackgroundHandler)
- `docker compose exec -T api php bin/console debug:container --parameter transformations.embedder_timeout_remove_background_ms` → `6000`
- Aucun test Phase 3 cassé (les tests qui asserent l'ancien comportement sont mis à jour, pas supprimés sans remplacement)
</verification>

<success_criteria>
- [ ] `RemoveBackgroundStepParams` DTO créé avec Assert\\Choice
- [ ] `StepParamsFactory` route REMOVE_BACKGROUND → RemoveBackgroundStepParams
- [ ] `RemoveBackgroundHandler` créé, étend AbstractEmbedderStepHandler, endpoint `/img/remove-background`
- [ ] services.yaml expose `transformations.embedder_timeout_remove_background_ms` (default 6000ms)
- [ ] `TransformationLookup::isAsyncStep` ne 404 plus pour REMOVE_BACKGROUND
- [ ] `TransformationLookup::isAsyncStep` 404 toujours pour ADD_BACKGROUND ai_prompt
- [ ] 4 tests `RemoveBackgroundStepParamsTest` verts (no skip)
- [ ] 4 tests `RemoveBackgroundHandlerTest` verts (no skip)
- [ ] 3 nouveaux tests `StepParamsFactoryTest` verts (routing + invalid + unknown key)
- [ ] 4 tests `TransformationLookupTest` verts
- [ ] Tests Phase 3 impactés mis à jour (PipelineRunnerTest::testUnsupportedStepTypeRaises géré)
- [ ] Aucune régression unit + integration
</success_criteria>

<output>
After completion, create `.planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-04-SUMMARY.md`
</output>
