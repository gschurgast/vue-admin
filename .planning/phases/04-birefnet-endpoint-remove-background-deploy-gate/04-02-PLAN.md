---
phase: 04
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - embedder/requirements.txt
  - embedder/scripts/download_models.py
  - embedder/core/bgremove_state.py
  - embedder/core/bgremove_models.py
  - embedder/core/log_json.py
  - embedder/routers/img_remove_background.py
  - embedder/app.py
  - embedder/tests/test_remove_background.py
autonomous: true
requirements: [IMGSVC-06, BGREMOVE-01, BGREMOVE-02, BGREMOVE-04, BGREMOVE-05]
user_setup: []

tags: [embedder, python, onnx, birefnet, isnet, asyncio]

must_haves:
  truths:
    - "POST /img/remove-background accepte multipart `image` + form `params` JSON et retourne PNG RGBA en 200"
    - "Le paramètre `model` accepte uniquement `birefnet` (défaut) | `isnet-general-use` ; tout autre enum → 422"
    - "Images > 4096px long-edge → 413 ; entre 2048² et 4096² → downscale auto à 2048 long-edge AVANT inference"
    - "Sortie PNG RGBA upscalée à la dimension originale (mask Lanczos)"
    - "asyncio.Lock module-level sérialise les inférences ; compteur inflight reflète l'occupation"
    - "Timeout 5s hard via asyncio.wait_for ; si fallbackOnTimeout=true → rerun isnet ; sinon → 504"
    - "ORT InferenceSession chargée 1x au startup, jamais par requête"
    - "Inference CPU-bound wrappée dans loop.run_in_executor(None, sess.run, ...) — loop jamais bloqué"
    - "Logs JSON structurés `{\"event\":\"remove_background\",...}` sur stdout"
  artifacts:
    - path: "embedder/routers/img_remove_background.py"
      provides: "FastAPI router POST /img/remove-background"
      min_lines: 80
      contains: "asyncio.Lock"
    - path: "embedder/core/bgremove_models.py"
      provides: "ORT session loaders + run_birefnet/run_isnet"
      min_lines: 80
      contains: "InferenceSession"
    - path: "embedder/core/bgremove_state.py"
      provides: "Module-level lock + inflight counter + last_inference_ms"
      contains: "asyncio.Lock"
    - path: "embedder/core/log_json.py"
      provides: "Helper log_event() JSON structuré"
    - path: "embedder/scripts/download_models.py"
      provides: "Script huggingface_hub snapshot_download + wget isnet"
  key_links:
    - from: "embedder/routers/img_remove_background.py"
      to: "embedder/core/bgremove_models.py"
      via: "from core.bgremove_models import run_birefnet, run_isnet"
      pattern: "from core.bgremove_models import"
    - from: "embedder/routers/img_remove_background.py"
      to: "embedder/core/bgremove_state.py"
      via: "from core.bgremove_state import lock, set_inflight, set_last_ms"
      pattern: "from core.bgremove_state import"
    - from: "embedder/app.py"
      to: "embedder/routers/img_remove_background.py"
      via: "app.include_router(img_remove_background.router)"
      pattern: "include_router\\(img_remove_background"
---

<objective>
Implémenter l'endpoint Python `POST /img/remove-background` complet : router FastAPI + Pydantic DTO + chargement ORT InferenceSession (BiRefNet FP16 + isnet) + sérialisation via `asyncio.Lock` + `run_in_executor` + downscale auto + timeout 5s avec fallback opt-in + logs JSON structurés.

Purpose: Livrer le moteur d'inférence de la phase. Tous les stubs xfail de Plan 04-01 doivent virer au vert.
Output: 12 tests xfail → passed (mockés), endpoint testable via `curl -F image=@x.png -F params='{"model":"birefnet"}' http://embedder:8000/img/remove-background -o out.png`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-CONTEXT.md
@.planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-RESEARCH.md
@.planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-VALIDATION.md
@.planning/phases/02-python-image-service-classical-endpoints/02-01-SUMMARY.md
@embedder/app.py
@embedder/routers/img_resize.py
@embedder/core/image_utils.py
@embedder/tests/conftest.py
@embedder/tests/test_remove_background.py

<interfaces>
<!-- Symboles déjà disponibles -->

From embedder/core/image_utils.py (Phase 2) :
- decode_image(raw: bytes) -> Image.Image     # EXIF + 50MPx + SVG reject
- image_response(img, fmt, quality=None, extra_headers=None) -> Response
- MAX_PIXELS = 50_000_000

From conftest.py (Plan 04-01) :
- client (TestClient session-scoped)
- product_2048_png, product_3000_jpg, product_4500_jpg, product_with_alpha_png
- mock_birefnet_session / mock_isnet_session (monkeypatch run_birefnet/run_isnet → uniform 128 mask)

From embedder/app.py :
- app = FastAPI(...)
- include_router pattern: `app.include_router(<module>.router)`
- ModelStatus enum (loaded|lazy|not_loaded|failed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Script download_models.py + bgremove_state + bgremove_models + log_json</name>
  <files>embedder/scripts/download_models.py, embedder/core/bgremove_state.py, embedder/core/bgremove_models.py, embedder/core/log_json.py</files>
  <read_first>
    - embedder/core/image_utils.py (pattern Phase 2 helpers — comment exposer du module-level state)
    - .planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-RESEARCH.md (Pattern 2 + Pattern 3 + Assumption A1 + Pitfall 2)
  </read_first>
  <behavior>
    - `get_birefnet()` charge `/app/models/birefnet/onnx/model_fp16.onnx` une SEULE fois (singleton)
    - `run_birefnet(img: Image.Image) -> Image.Image` retourne un masque L (greyscale) à la taille de img
    - `run_isnet(img)` idem avec normalization rembg (mean=0.5 std=1.0, post-norm min/max linear)
    - `lock = asyncio.Lock()` module-level (singleton process)
    - `set_inflight(+1/-1)` thread-safe via `threading.Lock`
    - `set_last_ms(int)` et `get_last_ms()` ; init `None`
    - `log_event(event, **fields)` écrit JSON sur stdout `{"event":<event>,"ts":<float>,...fields}`
    - Script `download_models.py` télécharge BiRefNet ONNX FP16 (HF `onnx-community/BiRefNet-ONNX`) + isnet (`rembg` release URL) dans `/models/`
  </behavior>
  <action>
    1. **Créer `embedder/core/log_json.py`** (copy verbatim from Research §"Structured JSON log helper") :
    ```python
    """Structured JSON logging to stdout (Datadog-compatible)."""
    from __future__ import annotations
    import json
    import logging
    import sys
    import time

    _log = logging.getLogger("embedder")

    def log_event(event: str, **fields) -> None:
        payload = {"event": event, "ts": time.time(), **fields}
        sys.stdout.write(json.dumps(payload, default=str) + "\n")
        sys.stdout.flush()
    ```

    2. **Créer `embedder/core/bgremove_state.py`** :
    ```python
    """Module-level singletons for the bg-remove endpoint (Phase 4, D-14)."""
    from __future__ import annotations
    import asyncio
    import threading

    # Single asyncio.Lock shared by BiRefNet + isnet inference paths (D-14).
    lock = asyncio.Lock()

    _inflight_lock = threading.Lock()
    _inflight: int = 0
    _last_inference_ms: int | None = None


    def set_inflight(delta: int) -> None:
        global _inflight
        with _inflight_lock:
            _inflight += delta


    def get_inflight() -> int:
        with _inflight_lock:
            return _inflight


    def set_last_ms(ms: int) -> None:
        global _last_inference_ms
        _last_inference_ms = ms


    def get_last_ms() -> int | None:
        return _last_inference_ms
    ```

    3. **Créer `embedder/core/bgremove_models.py`** — chargement ORT + preprocessing/postprocessing :
    ```python
    """Lazy-load ONNX sessions and run inference. CPU only (D-01)."""
    from __future__ import annotations
    import logging
    from pathlib import Path

    import numpy as np
    import onnxruntime as ort
    from PIL import Image

    MODELS_DIR = Path("/app/models")
    BIREFNET_PATH = MODELS_DIR / "birefnet" / "onnx" / "model_fp16.onnx"
    ISNET_PATH = MODELS_DIR / "isnet" / "isnet-general-use.onnx"

    # BiRefNet expects 1024x1024 with ImageNet normalization.
    BIREFNET_SIZE = 1024
    _BIREFNET_MEAN = np.array([0.485, 0.456, 0.406], dtype=np.float32).reshape(1, 3, 1, 1)
    _BIREFNET_STD  = np.array([0.229, 0.224, 0.225], dtype=np.float32).reshape(1, 3, 1, 1)

    # isnet-general-use (rembg) — mean=0.5 std=1.0
    ISNET_SIZE = 1024
    _ISNET_MEAN = 0.5
    _ISNET_STD  = 1.0

    _birefnet_session: ort.InferenceSession | None = None
    _isnet_session: ort.InferenceSession | None = None

    log = logging.getLogger("embedder")


    def _make_session(path: Path) -> ort.InferenceSession:
        so = ort.SessionOptions()
        so.inter_op_num_threads = 1   # we serialize via asyncio.Lock (D-14)
        return ort.InferenceSession(str(path), so, providers=["CPUExecutionProvider"])


    def get_birefnet() -> ort.InferenceSession:
        global _birefnet_session
        if _birefnet_session is None:
            log.info("Loading BiRefNet ONNX from %s", BIREFNET_PATH)
            _birefnet_session = _make_session(BIREFNET_PATH)
        return _birefnet_session


    def get_isnet() -> ort.InferenceSession:
        global _isnet_session
        if _isnet_session is None:
            log.info("Loading isnet ONNX from %s", ISNET_PATH)
            _isnet_session = _make_session(ISNET_PATH)
        return _isnet_session


    def _detect_input_dtype(sess: ort.InferenceSession) -> np.dtype:
        # A2: model_fp16.onnx accepts float16 inputs; FP32 model accepts float32.
        type_str = sess.get_inputs()[0].type   # e.g. 'tensor(float16)'
        return np.float16 if "float16" in type_str else np.float32


    def _preprocess_birefnet(img: Image.Image) -> np.ndarray:
        sess = get_birefnet()
        dtype = _detect_input_dtype(sess)
        resized = img.convert("RGB").resize((BIREFNET_SIZE, BIREFNET_SIZE), Image.Resampling.BILINEAR)
        arr = np.asarray(resized, dtype=np.float32) / 255.0
        arr = arr.transpose(2, 0, 1)[None, ...]            # NCHW
        arr = (arr - _BIREFNET_MEAN) / _BIREFNET_STD
        return arr.astype(dtype)


    def _preprocess_isnet(img: Image.Image) -> np.ndarray:
        sess = get_isnet()
        dtype = _detect_input_dtype(sess)
        resized = img.convert("RGB").resize((ISNET_SIZE, ISNET_SIZE), Image.Resampling.BILINEAR)
        arr = np.asarray(resized, dtype=np.float32) / 255.0
        arr = arr.transpose(2, 0, 1)[None, ...]            # NCHW
        arr = (arr - _ISNET_MEAN) / _ISNET_STD             # mean=0.5 std=1.0
        return arr.astype(dtype)


    def _sigmoid(x: np.ndarray) -> np.ndarray:
        return 1.0 / (1.0 + np.exp(-x.astype(np.float32)))


    def _postprocess_birefnet(raw_output: np.ndarray, dst_size: tuple[int, int]) -> Image.Image:
        # BiRefNet: sigmoid on the raw logit
        m = raw_output.squeeze()
        m = _sigmoid(m)
        m = (m * 255.0).clip(0, 255).astype(np.uint8)
        mask = Image.fromarray(m, mode="L")
        return mask.resize(dst_size, Image.Resampling.LANCZOS)


    def _postprocess_isnet(raw_output: np.ndarray, dst_size: tuple[int, int]) -> Image.Image:
        # isnet rembg: linear min/max normalisation (NOT sigmoid). See Assumption A1.
        m = raw_output.squeeze().astype(np.float32)
        mn, mx = float(m.min()), float(m.max())
        if mx - mn < 1e-6:
            arr = np.zeros_like(m, dtype=np.uint8)
        else:
            arr = ((m - mn) / (mx - mn) * 255.0).clip(0, 255).astype(np.uint8)
        mask = Image.fromarray(arr, mode="L")
        return mask.resize(dst_size, Image.Resampling.LANCZOS)


    def run_birefnet(img: Image.Image) -> Image.Image:
        """CPU-bound. Call ONLY via loop.run_in_executor(None, run_birefnet, img) (Pitfall 2)."""
        sess = get_birefnet()
        x = _preprocess_birefnet(img)
        input_name = sess.get_inputs()[0].name
        out = sess.run(None, {input_name: x})[0]
        return _postprocess_birefnet(out, img.size)


    def run_isnet(img: Image.Image) -> Image.Image:
        """CPU-bound. Call ONLY via loop.run_in_executor(None, run_isnet, img) (Pitfall 2)."""
        sess = get_isnet()
        x = _preprocess_isnet(img)
        input_name = sess.get_inputs()[0].name
        out = sess.run(None, {input_name: x})[0]
        return _postprocess_isnet(out, img.size)
    ```

    4. **Créer `embedder/scripts/download_models.py`** — invoqué par le Dockerfile (Plan 04-03) :
    ```python
    """Download BiRefNet + isnet ONNX models at Docker build time.

    Invoked from Dockerfile model-downloader stage. Idempotent ; uses
    huggingface_hub for BiRefNet and direct GitHub release URL for isnet.
    """
    from __future__ import annotations
    import os
    import sys
    import urllib.request
    from pathlib import Path

    from huggingface_hub import snapshot_download

    MODELS_DIR = Path(os.environ.get("MODELS_DIR", "/models"))

    BIREFNET_REPO = "onnx-community/BiRefNet-ONNX"
    BIREFNET_PATTERNS = ["onnx/model_fp16.onnx", "config.json"]

    ISNET_URL = "https://github.com/danielgatis/rembg/releases/download/v0.0.0/isnet-general-use.onnx"


    def download_birefnet() -> None:
        target = MODELS_DIR / "birefnet"
        target.mkdir(parents=True, exist_ok=True)
        snapshot_download(
            repo_id=BIREFNET_REPO,
            local_dir=str(target),
            allow_patterns=BIREFNET_PATTERNS,
        )
        print(f"[download_models] BiRefNet OK -> {target}")


    def download_isnet() -> None:
        target = MODELS_DIR / "isnet"
        target.mkdir(parents=True, exist_ok=True)
        out = target / "isnet-general-use.onnx"
        if out.exists() and out.stat().st_size > 100_000_000:
            print(f"[download_models] isnet already present ({out.stat().st_size} B), skip")
            return
        print(f"[download_models] Downloading isnet from {ISNET_URL} ...")
        urllib.request.urlretrieve(ISNET_URL, out)
        print(f"[download_models] isnet OK -> {out}")


    if __name__ == "__main__":
        download_birefnet()
        download_isnet()
        sys.exit(0)
    ```
  </action>
  <verify>
    <automated>test -f embedder/scripts/download_models.py && test -f embedder/core/bgremove_state.py && test -f embedder/core/bgremove_models.py && test -f embedder/core/log_json.py && grep -q "asyncio.Lock" embedder/core/bgremove_state.py && grep -q "InferenceSession" embedder/core/bgremove_models.py && grep -q "snapshot_download" embedder/scripts/download_models.py && docker compose exec -T embedder python -c "from core.bgremove_state import lock, get_inflight, get_last_ms; assert get_inflight() == 0 and get_last_ms() is None; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^lock = asyncio.Lock()" embedder/core/bgremove_state.py`
    - `grep -q "threading.Lock" embedder/core/bgremove_state.py` (compteur inflight thread-safe)
    - `grep -q "providers=\\[\"CPUExecutionProvider\"\\]" embedder/core/bgremove_models.py`
    - `grep -q "inter_op_num_threads = 1" embedder/core/bgremove_models.py`
    - `grep -q "BIREFNET_PATH = MODELS_DIR / \"birefnet\"" embedder/core/bgremove_models.py`
    - `grep -q "ISNET_PATH = MODELS_DIR / \"isnet\"" embedder/core/bgremove_models.py`
    - `grep -q "Image.Resampling.LANCZOS" embedder/core/bgremove_models.py`
    - `grep -q "snapshot_download" embedder/scripts/download_models.py`
    - `grep -q "onnx-community/BiRefNet-ONNX" embedder/scripts/download_models.py`
    - `grep -q "rembg/releases/download/v0.0.0/isnet-general-use.onnx" embedder/scripts/download_models.py`
    - Import test : `python -c "from core.bgremove_state import lock; from core.log_json import log_event"` réussit dans le container embedder
  </acceptance_criteria>
  <done>4 modules créés, importables, lock global module-level, sessions chargées à la demande via singleton, script de download idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Router img_remove_background.py + wiring app.py</name>
  <files>embedder/routers/img_remove_background.py, embedder/app.py, embedder/tests/test_remove_background.py</files>
  <read_first>
    - embedder/routers/img_resize.py (squelette router)
    - embedder/app.py (où ajouter include_router)
    - embedder/tests/test_remove_background.py (12 stubs xfail à dé-xfail)
    - .planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-RESEARCH.md (Pattern 1 verbatim)
  </read_first>
  <behavior>
    - POST `/img/remove-background` multipart `image` + form `params` JSON
    - Pydantic enum strict `model: Literal["birefnet", "isnet-general-use"] = "birefnet"`, `fallbackOnTimeout: bool = False`
    - 422 sur params invalides (Pydantic ValidationError)
    - 413 si max(img.size) > 4096 — AVANT toute inference
    - Downscale auto à 2048 long-edge si max > 2048 — `inference_img = img.copy()` puis `thumbnail` (Pitfall 3)
    - `async with lock:` + `set_inflight(+1)` / `-1` finally
    - Inference via `loop.run_in_executor(None, run_birefnet, inference_img)` (Pitfall 2)
    - Timeout 5s via `asyncio.wait_for(...)` (D-05)
    - Si TimeoutError + `fallbackOnTimeout=True` → run isnet, set `X-Model-Used: isnet-general-use`
    - Si TimeoutError + `fallbackOnTimeout=False` → 504 (D-04)
    - Output : mask upscalé à orig_size (Lanczos D-08), `img.convert("RGB").putalpha(mask_full)` (Pitfall 4)
    - Sauvegarde PNG, `media_type="image/png"`, headers `X-Render-Duration-Ms` + `X-Model-Used`
    - Log JSON `log_event("remove_background", model=..., latency_ms=..., image_dims=..., fallback_used=...)`
  </behavior>
  <action>
    1. **Créer `embedder/routers/img_remove_background.py`** (basé sur Research Pattern 1, adapté avec imports valides) :
    ```python
    """POST /img/remove-background — BiRefNet primary + isnet-general-use fallback (Phase 4, D-17)."""
    from __future__ import annotations

    import asyncio
    import io
    import time
    from typing import Literal

    from fastapi import APIRouter, File, Form, HTTPException, Response, UploadFile
    from PIL import Image
    from pydantic import BaseModel, ValidationError

    from core.image_utils import decode_image
    from core.bgremove_models import run_birefnet, run_isnet
    from core.bgremove_state import lock, set_inflight, set_last_ms
    from core.log_json import log_event

    router = APIRouter()

    MAX_DIM = 4096            # D-07
    DOWNSCALE_LONG_EDGE = 2048
    BIREFNET_TIMEOUT_S = 5.0  # D-05


    class RemoveBgParams(BaseModel):
        model: Literal["birefnet", "isnet-general-use"] = "birefnet"
        fallbackOnTimeout: bool = False


    @router.post("/img/remove-background")
    async def remove_background(image: UploadFile = File(...), params: str = Form(...)):
        try:
            p = RemoveBgParams.model_validate_json(params)
        except (ValidationError, ValueError) as exc:
            raise HTTPException(status_code=422, detail=str(exc))

        raw = await image.read()
        img = decode_image(raw)           # EXIF + 50 MPx + SVG reject (Phase 2)

        # D-07: 4K hard cap (stricter than the 50 MPx ceiling for this endpoint).
        if max(img.size) > MAX_DIM:
            raise HTTPException(status_code=413, detail=f"Image > {MAX_DIM}px long edge.")

        # D-07 + Pitfall 3: copy BEFORE thumbnail (in-place mutation otherwise).
        orig_size = img.size
        inference_img = img.copy()
        if max(orig_size) > DOWNSCALE_LONG_EDGE:
            inference_img.thumbnail(
                (DOWNSCALE_LONG_EDGE, DOWNSCALE_LONG_EDGE),
                Image.Resampling.LANCZOS,
            )

        fallback_used = False
        t0 = time.perf_counter()
        loop = asyncio.get_running_loop()

        async with lock:
            set_inflight(+1)
            try:
                if p.model == "birefnet":
                    try:
                        mask = await asyncio.wait_for(
                            loop.run_in_executor(None, run_birefnet, inference_img),
                            timeout=BIREFNET_TIMEOUT_S,
                        )
                    except asyncio.TimeoutError:
                        if not p.fallbackOnTimeout:
                            raise HTTPException(status_code=504, detail="BiRefNet timeout (>5s)")
                        mask = await loop.run_in_executor(None, run_isnet, inference_img)
                        fallback_used = True
                else:  # isnet-general-use explicit
                    mask = await loop.run_in_executor(None, run_isnet, inference_img)
            finally:
                set_inflight(-1)
                latency_ms = int((time.perf_counter() - t0) * 1000)
                set_last_ms(latency_ms)

        # D-08: upscale mask to original dims, then compose RGBA.
        mask_full = mask.resize(orig_size, Image.Resampling.LANCZOS)
        # Pitfall 4: force RGB to handle paletted / 1-bit / LA modes safely.
        rgba = img.convert("RGB")
        rgba.putalpha(mask_full)        # D-09: replaces any prior alpha

        log_event(
            "remove_background",
            model=p.model,
            latency_ms=latency_ms,
            image_dims=f"{orig_size[0]}x{orig_size[1]}",
            fallback_used=fallback_used,
        )

        buf = io.BytesIO()
        rgba.save(buf, "PNG")
        return Response(
            content=buf.getvalue(),
            media_type="image/png",
            headers={
                "X-Render-Duration-Ms": str(latency_ms),
                "X-Model-Used": "isnet-general-use" if fallback_used else p.model,
            },
        )
    ```

    2. **Modifier `embedder/app.py`** :
       - Ajouter `img_remove_background` à l'import groupé (autour ligne 37-43)
       - Ajouter `app.include_router(img_remove_background.router)` à la suite des autres include_router (ligne ~49)

    3. **Retirer le `pytestmark = pytest.mark.xfail` global** de `embedder/tests/test_remove_background.py` (les tests doivent maintenant PASSER, plus xfail). Si un test individuel reste à câbler (notamment `test_birefnet_timeout_falls_back_to_isnet` qui nécessite un monkeypatch précis), garder son xfail individuel — voir Task 3.

    4. **Ajuster `test_birefnet_timeout_falls_back_to_isnet`** pour patcher `asyncio.wait_for` (qui doit lever TimeoutError) :
       ```python
       def test_birefnet_timeout_falls_back_to_isnet(client, product_2048_png, mock_birefnet_session, monkeypatch):
           import asyncio as _aio
           import routers.img_remove_background as rbg

           async def _raise_timeout(*args, **kwargs):
               raise _aio.TimeoutError()
           monkeypatch.setattr(rbg.asyncio, "wait_for", _raise_timeout)

           r = client.post("/img/remove-background",
                           files={"image": ("p.png", product_2048_png, "image/png")},
                           data={"params": '{"model":"birefnet","fallbackOnTimeout":true}'})
           assert r.status_code == 200
           assert r.headers.get("X-Model-Used") == "isnet-general-use"
       ```

    5. **Ajuster `test_timeout_without_fallback_returns_504`** identiquement (patch wait_for → 504) :
       ```python
       def test_timeout_without_fallback_returns_504(client, product_2048_png, mock_birefnet_session, monkeypatch):
           import asyncio as _aio
           import routers.img_remove_background as rbg

           async def _raise_timeout(*args, **kwargs):
               raise _aio.TimeoutError()
           monkeypatch.setattr(rbg.asyncio, "wait_for", _raise_timeout)

           r = client.post("/img/remove-background",
                           files={"image": ("p.png", product_2048_png, "image/png")},
                           data={"params": '{"model":"birefnet","fallbackOnTimeout":false}'})
           assert r.status_code == 504
       ```

    6. **Ajuster `test_lock_serializes_inflight`** pour vérifier le compteur :
       ```python
       def test_lock_serializes_inflight(client, product_2048_png, mock_birefnet_session):
           from core.bgremove_state import get_inflight
           assert get_inflight() == 0  # baseline
           r = client.post("/img/remove-background",
                           files={"image": ("p.png", product_2048_png, "image/png")},
                           data={"params": '{"model":"birefnet"}'})
           assert r.status_code == 200
           # After request completes, inflight returns to 0 (decremented in finally).
           assert get_inflight() == 0
       ```

    7. **Ajuster `test_birefnet_session_loads`** : ce test attend `loaded` mais dans Plan 04-02 (avant /health enrichi Plan 04-03), `/health` n'inclut pas encore birefnet. **Maintenir xfail** pour ce test seul (sera vert après Plan 04-03) :
       ```python
       @pytest.mark.xfail(reason="Plan 04-03 enriches /health with birefnet.status")
       def test_birefnet_session_loads(client):
           ...
       ```
  </action>
  <verify>
    <automated>docker compose exec -T embedder pytest -m "not integration_ml" tests/test_remove_background.py -v 2>&1 | tail -20 && grep -q "include_router(img_remove_background.router)" embedder/app.py && grep -q "async with lock:" embedder/routers/img_remove_background.py && grep -q "loop.run_in_executor(None, run_birefnet" embedder/routers/img_remove_background.py && grep -q "asyncio.wait_for" embedder/routers/img_remove_background.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "include_router(img_remove_background" embedder/app.py`
    - `grep -q "async with lock:" embedder/routers/img_remove_background.py`
    - `grep -q "loop.run_in_executor(None, run_birefnet" embedder/routers/img_remove_background.py`
    - `grep -q "asyncio.wait_for" embedder/routers/img_remove_background.py`
    - `grep -q "MAX_DIM = 4096" embedder/routers/img_remove_background.py`
    - `grep -q "DOWNSCALE_LONG_EDGE = 2048" embedder/routers/img_remove_background.py`
    - `grep -q "Literal\[\"birefnet\", \"isnet-general-use\"\]" embedder/routers/img_remove_background.py`
    - `grep -q "set_inflight(+1)" embedder/routers/img_remove_background.py` (et `-1` finally)
    - `grep -q "img.convert(\"RGB\")" embedder/routers/img_remove_background.py` (Pitfall 4)
    - `grep -q "inference_img = img.copy()" embedder/routers/img_remove_background.py` (Pitfall 3)
    - `docker compose exec -T embedder pytest -m "not integration_ml" tests/test_remove_background.py -v` : 11 passed, 1 xfailed (`test_birefnet_session_loads`) — soit 12 collectés, 0 erreur, 0 failed
    - `docker compose exec -T embedder pytest tests/test_decode_image.py` (Phase 2) reste vert — pas de régression
  </acceptance_criteria>
  <done>Endpoint POST /img/remove-background opérationnel, 11/12 stubs verts (1 xfail Plan 04-03), lock + wait_for + run_in_executor câblés, pas de régression Phase 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 04-02)

| Boundary | Description |
|----------|-------------|
| HTTP client (PHP orchestrator) → embedder | Multipart `image` (untrusted bytes) + form `params` (untrusted JSON) ; réseau interne mais bytes attaquables (image bomb, params injection) |
| Disk → ORT runtime | Modèles ONNX bakés dans l'image (immutable). Pas de chemin utilisateur. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | DoS | Inférences concurrentes saturant CPU/RAM | mitigate | `asyncio.Lock` module-level + compteur `inflight` exposé via /health (Plan 04-03). 1 inférence simultanée par process. |
| T-04-02 | Spoofing/Injection | Param `model` injecté hors enum | mitigate | Pydantic `Literal["birefnet","isnet-general-use"]` ; tout autre → 422 ; testé par `test_unknown_model_rejected_422`. |
| T-04-03 | DoS | Image bomb (> 4K, ou décompression-bomb compressed PNG) | mitigate | `decode_image()` Phase 2 rejette 50 MPx ; 413 explicite > 4096px AVANT inference. Downscale auto à 2048 réduit RAM/CPU avant l'ORT call. |
| T-04-06 | Resource leak | InferenceSession instancié par requête | mitigate | Singleton module-level `_birefnet_session` / `_isnet_session` ; chargé 1x ; pas de recréation. |
| T-04-07 | Path traversal | filename multipart utilisé pour écrire sur disque | mitigate | Aucune écriture disque — tout in-memory via `io.BytesIO`. |
</threat_model>

<verification>
- Endpoint répond 200 sur image valide : `docker compose exec -T embedder pytest -m "not integration_ml" tests/test_remove_background.py -v` → 11 passed + 1 xfailed
- Endpoint répond 413 sur image > 4K
- Endpoint répond 422 sur model inconnu
- Endpoint répond 504 sur timeout sans fallback (mocké)
- Endpoint applique le fallback isnet sur timeout avec fallback=true (mocké)
- Aucune régression sur les tests Phase 2 : `docker compose exec -T embedder pytest -m "not integration_ml" tests/test_decode_image.py tests/test_resize.py` → all pass
- `grep -q "import img_remove_background" embedder/app.py` ou équivalent dans le block `from routers import`
</verification>

<success_criteria>
- [ ] `embedder/core/bgremove_state.py` créé avec `lock = asyncio.Lock()` + compteur inflight thread-safe
- [ ] `embedder/core/bgremove_models.py` créé avec `_birefnet_session`/`_isnet_session` singletons + `run_birefnet`/`run_isnet` + pre/post processing par modèle
- [ ] `embedder/core/log_json.py` créé avec `log_event()` JSON stdout
- [ ] `embedder/scripts/download_models.py` créé, utilise `huggingface_hub.snapshot_download` pour BiRefNet et `urllib.request` pour isnet
- [ ] `embedder/routers/img_remove_background.py` créé, conforme à Research Pattern 1 (Lock + wait_for + run_in_executor + 413 + downscale + 504 + fallback + RGBA)
- [ ] `embedder/app.py` inclut le router
- [ ] 11/12 tests Python passent, 1 xfailed (`test_birefnet_session_loads` — sera vert Plan 04-03)
- [ ] Aucune régression Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-02-SUMMARY.md`
</output>
