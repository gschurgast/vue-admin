---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - embedder/requirements.txt
  - embedder/pytest.ini
  - embedder/tests/conftest.py
  - embedder/tests/fixtures/product_2048.png
  - embedder/tests/fixtures/product_3000.jpg
  - embedder/tests/fixtures/product_4500.jpg
  - embedder/tests/fixtures/product_with_alpha.png
  - embedder/tests/test_remove_background.py
  - embedder/tests/test_remove_background_e2e.py
  - embedder/tests/test_health.py
  - api/tests/Unit/Service/AssetTransformation/StepHandler/RemoveBackgroundHandlerTest.php
  - api/tests/Unit/Service/AssetTransformation/StepParams/RemoveBackgroundStepParamsTest.php
autonomous: true
requirements: [IMGSVC-06, BGREMOVE-01, BGREMOVE-02, BGREMOVE-03, BGREMOVE-04, BGREMOVE-05, BGREMOVE-06]
user_setup: []

must_haves:
  truths:
    - "Pytest marker `integration_ml` est déclaré et collectable (vs OFF par défaut)"
    - "Stubs Python xfail (12 fonctions) collectent via pytest --collect-only sans ImportError"
    - "Stubs PHPUnit (2 classes vides ou skipped) collectent sans erreur"
    - "Fixtures images checked-in (4 fichiers) existent et sont lisibles par Pillow"
    - "onnxruntime==1.22.0 pinné dans requirements.txt"
  artifacts:
    - path: "embedder/requirements.txt"
      provides: "Pin ONNX Runtime + huggingface_hub"
      contains: "onnxruntime==1.22.0"
    - path: "embedder/pytest.ini"
      provides: "integration_ml marker registered"
      contains: "integration_ml"
    - path: "embedder/tests/test_remove_background.py"
      provides: "Stubs (xfail) pour tous les comportements remove_background — REMPLIS Wave 1"
      min_lines: 50
    - path: "embedder/tests/test_remove_background_e2e.py"
      provides: "Stubs E2E (skip/integration_ml) — exécutés Webfacto-only"
    - path: "embedder/tests/test_health.py"
      provides: "Test étendu /health (birefnet/isnet) — déjà existe Phase 2, à compléter"
    - path: "embedder/tests/conftest.py"
      provides: "Fixtures product_*.{png,jpg}, mock_birefnet_session, mock_isnet_session, temp_models_dir"
    - path: "api/tests/Unit/Service/AssetTransformation/StepHandler/RemoveBackgroundHandlerTest.php"
      provides: "Stub MockHttpClient pour RemoveBackgroundHandler — REMPLI Plan 04-04"
    - path: "api/tests/Unit/Service/AssetTransformation/StepParams/RemoveBackgroundStepParamsTest.php"
      provides: "Stub DTO assertion — REMPLI Plan 04-04"
  key_links:
    - from: "embedder/pytest.ini"
      to: "embedder/tests/test_remove_background.py"
      via: "marker integration_ml + asyncio_mode auto"
      pattern: "integration_ml"
    - from: "embedder/tests/test_remove_background.py"
      to: "embedder/tests/conftest.py"
      via: "fixtures monkeypatched (mock_birefnet_session, product_2048_png)"
      pattern: "@pytest.fixture"
---

<objective>
Wave 0 — Installer toute l'infrastructure de test de Phase 4 SANS implémentation : pin ONNX Runtime 1.22.0 (bug #26261), marker `integration_ml`, 4 fixtures images, 12 stubs pytest (xfail/skip) + 2 stubs PHPUnit, fichier conftest avec mocks ONNX session.

Purpose: Établir le filet Nyquist avant Wave 1. Aucun test ne peut être MISSING en aval ; tout test sera juste "xfail jusqu'à implémentation".
Output: Tests collectables et rouges (xfail) sur tout le périmètre Phase 4, prêts à virer au vert au fur et à mesure des plans 02 → 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-CONTEXT.md
@.planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-RESEARCH.md
@.planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-VALIDATION.md

# Plan 02-01 SUMMARY pour les conventions de fixtures embedder
@.planning/phases/02-python-image-service-classical-endpoints/02-01-SUMMARY.md

# Files actuels à étendre
@embedder/requirements.txt
@embedder/pytest.ini
@embedder/app.py

<interfaces>
<!-- Symboles déjà disponibles dans le projet, à NE PAS recréer -->

From embedder/core/image_utils.py (Phase 2) :
- decode_image(raw: bytes) -> Image.Image     # déjà testée, applique EXIF + 50 MPx
- image_response(img, fmt, quality=None, extra_headers=None) -> Response
- MAX_PIXELS = 50_000_000

From api/src/Service/AssetTransformation/StepHandler/AbstractEmbedderStepHandler.php :
- abstract protected function endpointPath(): string;
- public function run(string $bytes, array $params, int $timeoutMs): HandlerResult;
- defaultTimeoutMs(): int

Pytest stack disponible (Phase 2):
- pytest 9.0.3 + pytest-asyncio 1.4.0 + httpx 0.28.1
- asyncio_mode = auto
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin ONNX Runtime + déclarer marker integration_ml + ajouter huggingface_hub</name>
  <files>embedder/requirements.txt, embedder/pytest.ini</files>
  <read_first>
    - embedder/requirements.txt (Phase 2 baseline)
    - embedder/pytest.ini (Phase 2 baseline : asyncio_mode=auto, testpaths=tests)
    - .planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-RESEARCH.md (Standard Stack section, Pitfall 1)
  </read_first>
  <action>
    1. Modifier `embedder/requirements.txt` en AJOUTANT (préserver tout l'existant) ces deux lignes EXACTEMENT :
    ```
    onnxruntime==1.22.0
    huggingface_hub>=0.27,<1
    ```
    Justification commentaire au-dessus de `onnxruntime==1.22.0` : `# Pinned to 1.22.0 — ORT 1.23+ has external-data load bug (github.com/microsoft/onnxruntime/issues/26261) which affects BiRefNet ONNX.`
    NE PAS toucher aux pins existants (`pillow-avif-plugin==1.5.5`, `torch==2.12.0+cpu`, etc.).

    2. Modifier `embedder/pytest.ini` pour ajouter la section `markers` :
    ```ini
    [pytest]
    asyncio_mode = auto
    testpaths = tests
    markers =
        integration_ml: requires real ONNX models (~1GB, slow). Run via `pytest -m integration_ml`. Default OFF.
    ```

    3. Rebuild de l'image embedder NON requis dans cette tâche (la tâche 2 + 3 utilisent juste les stubs).
  </action>
  <verify>
    <automated>grep -q "onnxruntime==1.22.0" embedder/requirements.txt && grep -q "huggingface_hub" embedder/requirements.txt && grep -q "integration_ml" embedder/pytest.ini</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^onnxruntime==1.22.0" embedder/requirements.txt` = 1
    - `grep -c "^huggingface_hub" embedder/requirements.txt` = 1
    - `grep -q "integration_ml: requires real ONNX" embedder/pytest.ini`
    - `pip install --dry-run -r embedder/requirements.txt` réussit (résoudre les versions) — vérification facultative manuelle si docker dispo
  </acceptance_criteria>
  <done>requirements.txt pinned, pytest.ini déclare le marker, aucune ligne existante perdue.</done>
</task>

<task type="auto">
  <name>Task 2: Fixtures images checked-in + conftest.py avec mocks ONNX</name>
  <files>embedder/tests/fixtures/product_2048.png, embedder/tests/fixtures/product_3000.jpg, embedder/tests/fixtures/product_4500.jpg, embedder/tests/fixtures/product_with_alpha.png, embedder/tests/conftest.py</files>
  <read_first>
    - embedder/tests/conftest.py (s'il existe ; sinon créer)
    - embedder/tests/test_decode_image.py (référence pour fixtures Phase 2)
    - .planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-VALIDATION.md (Wave 0 Requirements)
    - .planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-RESEARCH.md (Pattern 2 + Test infrastructure CI note)
  </read_first>
  <action>
    1. **Générer les 4 fixtures images** programmatiquement (au lieu d'images "réelles" Habitat — Webfacto signera après) via un petit script de génération **éphémère** exécuté localement OU directement écrites en bytes Pillow. Approche recommandée : créer un mini-utilitaire Python jetable qui produit les 4 fichiers, ou utiliser Pillow inline. Toutes les images doivent être colorées (NON uniformes) pour permettre des assertions futures sur la non-trivialité du masque.
       - `product_2048.png` : 2048×2048 RGB (random noise + 1 gradient diagonal pour avoir des "edges" exploitables par BiRefNet). Taille fichier visée < 5 MB.
       - `product_3000.jpg` : 3000×2400 JPEG quality 85 (testera downscale auto à 2048 long-edge).
       - `product_4500.jpg` : 4500×3000 JPEG quality 80 (testera 413 > 4K cap).
       - `product_with_alpha.png` : 1024×1024 RGBA avec alpha déjà non-trivial (testera D-09 — alpha pré-existant remplacé).
       
       Cmd suggérée (à exécuter `docker compose exec embedder python -c "..."` ou équivalent local) :
       ```python
       from PIL import Image
       import numpy as np
       import os
       os.makedirs("embedder/tests/fixtures", exist_ok=True)
       rng = np.random.default_rng(42)
       def make_rgb(w, h):
           arr = rng.integers(0, 255, (h, w, 3), dtype=np.uint8)
           # add diagonal gradient for edges
           grad = np.linspace(0, 255, max(w, h)).astype(np.uint8)
           for i in range(min(w, h)):
               arr[i, i] = [grad[i], 255 - grad[i], grad[i] // 2]
           return Image.fromarray(arr, "RGB")
       make_rgb(2048, 2048).save("embedder/tests/fixtures/product_2048.png", "PNG", optimize=True)
       make_rgb(3000, 2400).save("embedder/tests/fixtures/product_3000.jpg", "JPEG", quality=85)
       make_rgb(4500, 3000).save("embedder/tests/fixtures/product_4500.jpg", "JPEG", quality=80)
       rgba = make_rgb(1024, 1024).convert("RGBA")
       alpha = Image.new("L", (1024, 1024), color=200)
       rgba.putalpha(alpha)
       rgba.save("embedder/tests/fixtures/product_with_alpha.png", "PNG")
       ```
       Les fichiers sont **commités** dans le repo (binaire git).

    2. **Créer/étendre `embedder/tests/conftest.py`** avec ces fixtures et mocks :
       ```python
       from __future__ import annotations
       import io
       from pathlib import Path
       import pytest
       from PIL import Image
       from fastapi.testclient import TestClient
       from app import app

       FIXTURES = Path(__file__).parent / "fixtures"

       @pytest.fixture(scope="session")
       def client():
           return TestClient(app)

       def _read_bytes(name: str) -> bytes:
           return (FIXTURES / name).read_bytes()

       @pytest.fixture
       def product_2048_png() -> bytes:
           return _read_bytes("product_2048.png")

       @pytest.fixture
       def product_3000_jpg() -> bytes:
           return _read_bytes("product_3000.jpg")

       @pytest.fixture
       def product_4500_jpg() -> bytes:
           return _read_bytes("product_4500.jpg")

       @pytest.fixture
       def product_with_alpha_png() -> bytes:
           return _read_bytes("product_with_alpha.png")

       @pytest.fixture
       def mock_birefnet_session(monkeypatch):
           """Replace run_birefnet with a trivial mask returning uniform 128.
           Used by tests that exercise routing/multipart/headers without loading 1GB."""
           def _fake(img: Image.Image) -> Image.Image:
               return Image.new("L", img.size, color=128)
           # core.bgremove_models is created in Plan 04-02; the import-time
           # monkeypatch must tolerate the absence of the module until then.
           try:
               import core.bgremove_models as m
               monkeypatch.setattr(m, "run_birefnet", _fake)
               monkeypatch.setattr(m, "run_isnet", _fake)
           except ImportError:
               pytest.skip("core.bgremove_models not yet implemented (Plan 04-02)")
           return _fake

       @pytest.fixture
       def mock_isnet_session(mock_birefnet_session):
           # alias — same fake under the hood
           return mock_birefnet_session
       ```
       Si un `conftest.py` existe déjà, MERGE (ne pas écraser) — ajoute uniquement les nouvelles fixtures.

    3. Vérifier que la collection pytest n'échoue pas même si `core.bgremove_models` n'existe pas (le `pytest.skip` dans `mock_birefnet_session` gère ça).
  </action>
  <verify>
    <automated>ls embedder/tests/fixtures/product_2048.png embedder/tests/fixtures/product_3000.jpg embedder/tests/fixtures/product_4500.jpg embedder/tests/fixtures/product_with_alpha.png && grep -q "mock_birefnet_session" embedder/tests/conftest.py && grep -q "product_2048_png" embedder/tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - 4 fichiers fixtures existent dans `embedder/tests/fixtures/`
    - Chaque fixture s'ouvre via `PIL.Image.open()` sans erreur (taille attendue : 2048×2048, 3000×2400, 4500×3000, 1024×1024 RGBA)
    - `grep -q "product_2048_png" embedder/tests/conftest.py`
    - `grep -q "mock_birefnet_session" embedder/tests/conftest.py`
    - `grep -q "import core.bgremove_models" embedder/tests/conftest.py` (l'import lazy + skip)
    - `docker compose exec embedder pytest --collect-only -q 2>&1 | tail -5` ne crash pas (collection passe ; xfail/skip permis)
  </acceptance_criteria>
  <done>Fixtures binaires commitables + conftest avec mock_birefnet_session/mock_isnet_session + 4 product_* fixtures + client TestClient session-scoped.</done>
</task>

<task type="auto">
  <name>Task 3: Stubs xfail/skip — 12 tests Python + 2 tests PHP</name>
  <files>embedder/tests/test_remove_background.py, embedder/tests/test_remove_background_e2e.py, embedder/tests/test_health.py, api/tests/Unit/Service/AssetTransformation/StepHandler/RemoveBackgroundHandlerTest.php, api/tests/Unit/Service/AssetTransformation/StepParams/RemoveBackgroundStepParamsTest.php</files>
  <read_first>
    - .planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-VALIDATION.md (Per-Task Verification Map lignes 50-70)
    - api/tests/Unit/Service/AssetTransformation/StepHandler/HandlersHttpTest.php (référence MockHttpClient PHP Phase 3)
    - api/tests/Unit/Service/AssetTransformation/StepParams/StepParamsFactoryTest.php (référence pattern DTO tests Phase 3)
  </read_first>
  <action>
    1. **Créer `embedder/tests/test_remove_background.py`** avec ces 12 stubs xfail (un test = un comportement de VALIDATION.md task map 04-XX-01 → 04-XX-09 + structured log + concurrent serialization) :
    ```python
    """Phase 4 — POST /img/remove-background. STUBS Wave 0: xfail until Plan 04-02 implements."""
    from __future__ import annotations
    import pytest

    pytestmark = pytest.mark.xfail(reason="Plan 04-02 will implement POST /img/remove-background")

    def test_birefnet_session_loads(client):
        r = client.get("/health")
        assert r.json()["models"]["birefnet"]["status"] == "loaded"

    def test_remove_background_birefnet_returns_png_rgba(client, product_2048_png, mock_birefnet_session):
        r = client.post("/img/remove-background",
                        files={"image": ("p.png", product_2048_png, "image/png")},
                        data={"params": '{"model":"birefnet"}'})
        assert r.status_code == 200
        assert r.headers["content-type"] == "image/png"

    def test_default_model_is_birefnet(client, product_2048_png, mock_birefnet_session):
        r = client.post("/img/remove-background",
                        files={"image": ("p.png", product_2048_png, "image/png")},
                        data={"params": "{}"})
        assert r.status_code == 200
        assert r.headers.get("X-Model-Used") == "birefnet"

    def test_isnet_explicit_selection(client, product_2048_png, mock_isnet_session):
        r = client.post("/img/remove-background",
                        files={"image": ("p.png", product_2048_png, "image/png")},
                        data={"params": '{"model":"isnet-general-use"}'})
        assert r.status_code == 200

    def test_unknown_model_rejected_422(client, product_2048_png):
        r = client.post("/img/remove-background",
                        files={"image": ("p.png", product_2048_png, "image/png")},
                        data={"params": '{"model":"rmbg-1.4"}'})
        assert r.status_code == 422

    def test_birefnet_timeout_falls_back_to_isnet(client, product_2048_png, monkeypatch):
        # Plan 04-02 will define the patching hook for asyncio.wait_for
        r = client.post("/img/remove-background",
                        files={"image": ("p.png", product_2048_png, "image/png")},
                        data={"params": '{"model":"birefnet","fallbackOnTimeout":true}'})
        assert r.status_code == 200
        assert r.headers.get("X-Model-Used") == "isnet-general-use"

    def test_timeout_without_fallback_returns_504(client, product_2048_png):
        r = client.post("/img/remove-background",
                        files={"image": ("p.png", product_2048_png, "image/png")},
                        data={"params": '{"model":"birefnet","fallbackOnTimeout":false}'})
        assert r.status_code == 504

    def test_image_over_4k_returns_413(client, product_4500_jpg):
        r = client.post("/img/remove-background",
                        files={"image": ("p.jpg", product_4500_jpg, "image/jpeg")},
                        data={"params": '{"model":"birefnet"}'})
        assert r.status_code == 413

    def test_image_3000px_downscaled_then_upscaled(client, product_3000_jpg, mock_birefnet_session):
        from PIL import Image
        import io
        r = client.post("/img/remove-background",
                        files={"image": ("p.jpg", product_3000_jpg, "image/jpeg")},
                        data={"params": '{"model":"birefnet"}'})
        assert r.status_code == 200
        out = Image.open(io.BytesIO(r.content))
        # Output must be at ORIGINAL dims (3000×2400), not the inference 2048
        assert out.size == (3000, 2400)

    def test_output_is_png_rgba(client, product_2048_png, mock_birefnet_session):
        from PIL import Image
        import io
        r = client.post("/img/remove-background",
                        files={"image": ("p.png", product_2048_png, "image/png")},
                        data={"params": '{"model":"birefnet"}'})
        out = Image.open(io.BytesIO(r.content))
        assert out.mode == "RGBA"

    def test_rgba_input_alpha_replaced(client, product_with_alpha_png, mock_birefnet_session):
        from PIL import Image
        import io
        r = client.post("/img/remove-background",
                        files={"image": ("p.png", product_with_alpha_png, "image/png")},
                        data={"params": '{"model":"birefnet"}'})
        out = Image.open(io.BytesIO(r.content))
        assert out.mode == "RGBA"  # alpha replaced, not the original 200-uniform one

    def test_lock_serializes_inflight(client, product_2048_png, mock_birefnet_session):
        # Concurrent inflight via asyncio.gather — plan 04-02 wire le compteur
        # Cette assertion exacte sera affinée en Plan 04-02
        r1 = client.post("/img/remove-background",
                         files={"image": ("p.png", product_2048_png, "image/png")},
                         data={"params": '{"model":"birefnet"}'})
        assert r1.status_code == 200

    def test_structured_log_emitted(client, product_2048_png, mock_birefnet_session, capsys):
        r = client.post("/img/remove-background",
                        files={"image": ("p.png", product_2048_png, "image/png")},
                        data={"params": '{"model":"birefnet"}'})
        out = capsys.readouterr().out
        assert '"event": "remove_background"' in out
    ```

    2. **Créer `embedder/tests/test_remove_background_e2e.py`** (skippé par défaut — gated par marker integration_ml) :
    ```python
    """Phase 4 — E2E heavy tests with REAL BiRefNet ONNX (~1GB). RUN ONLY VIA pytest -m integration_ml."""
    from __future__ import annotations
    import pytest

    pytestmark = pytest.mark.integration_ml

    def test_birefnet_real_inference(client, product_2048_png):
        r = client.post("/img/remove-background",
                        files={"image": ("p.png", product_2048_png, "image/png")},
                        data={"params": '{"model":"birefnet"}'})
        assert r.status_code == 200
        # Real BiRefNet ⇒ non-trivial alpha mask
        from PIL import Image
        import io, numpy as np
        out = Image.open(io.BytesIO(r.content))
        alpha = np.array(out.split()[3])
        # At least 5% pixels are transparent (mask is non-degenerate)
        assert (alpha < 16).mean() >= 0.05

    def test_birefnet_latency_p95(client, product_2048_png):
        import time
        durations = []
        for _ in range(10):
            t0 = time.perf_counter()
            r = client.post("/img/remove-background",
                            files={"image": ("p.png", product_2048_png, "image/png")},
                            data={"params": '{"model":"birefnet"}'})
            assert r.status_code == 200
            durations.append((time.perf_counter() - t0) * 1000)
        durations.sort()
        p95 = durations[int(0.95 * len(durations)) - 1]
        # D-13 checklist item 4: p95 < 3000ms
        assert p95 < 3000.0, f"p95 latency {p95:.0f}ms > 3000ms"
    ```

    3. **Étendre `embedder/tests/test_health.py`** (le fichier existe déjà depuis Phase 2). Lire d'abord pour ne pas casser les 3 tests existants. AJOUTER ces deux stubs xfail à la fin du fichier :
    ```python
    import pytest

    @pytest.mark.xfail(reason="Plan 04-03 will enrich /health with birefnet & isnet status")
    def test_health_includes_birefnet_status(client):
        r = client.get("/health")
        m = r.json()["models"]
        assert m["birefnet"]["status"] == "loaded"
        assert "inflight" in m["birefnet"]
        assert "last_inference_ms" in m["birefnet"]
        assert m["isnet"]["status"] == "loaded"

    @pytest.mark.xfail(reason="Plan 04-03 will set status=degraded when birefnet not loaded")
    def test_health_degraded_when_birefnet_not_loaded(client, monkeypatch):
        import core.bgremove_models as m
        monkeypatch.setattr(m, "_birefnet_session", None)
        r = client.get("/health")
        assert r.json()["status"] == "degraded"
    ```

    4. **Créer `api/tests/Unit/Service/AssetTransformation/StepHandler/RemoveBackgroundHandlerTest.php`** (stub PHPUnit, skipped) — pattern HandlersHttpTest :
    ```php
    <?php

    namespace App\Tests\Unit\Service\AssetTransformation\StepHandler;

    use PHPUnit\Framework\TestCase;

    /**
     * @group phase-04
     * Stubs created in Plan 04-01 Wave 0. Plan 04-04 implements RemoveBackgroundHandler.
     */
    final class RemoveBackgroundHandlerTest extends TestCase
    {
        public function testHandlerPostsToRemoveBackgroundEndpoint(): void
        {
            $this->markTestSkipped('Plan 04-04 will implement RemoveBackgroundHandler.');
        }

        public function testHandlerReturnsHandlerResultWithPngContentType(): void
        {
            $this->markTestSkipped('Plan 04-04 will implement RemoveBackgroundHandler.');
        }

        public function testHandlerDoesNotRetryOn504(): void
        {
            $this->markTestSkipped('Plan 04-04 will implement RemoveBackgroundHandler.');
        }
    }
    ```

    5. **Créer `api/tests/Unit/Service/AssetTransformation/StepParams/RemoveBackgroundStepParamsTest.php`** (stub) :
    ```php
    <?php

    namespace App\Tests\Unit\Service\AssetTransformation\StepParams;

    use PHPUnit\Framework\TestCase;

    /**
     * @group phase-04
     * Stubs created in Plan 04-01 Wave 0. Plan 04-04 implements RemoveBackgroundStepParams.
     */
    final class RemoveBackgroundStepParamsTest extends TestCase
    {
        public function testDefaultModelIsBirefnet(): void
        {
            $this->markTestSkipped('Plan 04-04 will implement RemoveBackgroundStepParams.');
        }

        public function testInvalidModelRejected(): void
        {
            $this->markTestSkipped('Plan 04-04 will implement RemoveBackgroundStepParams.');
        }

        public function testFallbackOnTimeoutDefaultsToFalse(): void
        {
            $this->markTestSkipped('Plan 04-04 will implement RemoveBackgroundStepParams.');
        }
    }
    ```
  </action>
  <verify>
    <automated>docker compose exec -T embedder pytest --collect-only -q tests/test_remove_background.py tests/test_remove_background_e2e.py tests/test_health.py 2>&1 | tail -5 && docker compose exec -T api vendor/bin/phpunit --filter "RemoveBackgroundHandlerTest|RemoveBackgroundStepParamsTest" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `embedder/tests/test_remove_background.py` contient EXACTEMENT 12 fonctions `def test_*` (grep `grep -c "^def test_" embedder/tests/test_remove_background.py` → 12)
    - `embedder/tests/test_remove_background_e2e.py` contient 2 fonctions test marquées `integration_ml`
    - `grep -q "pytestmark = pytest.mark.integration_ml" embedder/tests/test_remove_background_e2e.py`
    - `grep -q "pytestmark = pytest.mark.xfail" embedder/tests/test_remove_background.py`
    - `embedder/tests/test_health.py` contient au moins `test_health_includes_birefnet_status` et `test_health_degraded_when_birefnet_not_loaded`
    - `api/tests/Unit/Service/AssetTransformation/StepHandler/RemoveBackgroundHandlerTest.php` existe, `markTestSkipped` présent
    - `api/tests/Unit/Service/AssetTransformation/StepParams/RemoveBackgroundStepParamsTest.php` existe, `markTestSkipped` présent
    - `docker compose exec -T embedder pytest --collect-only` retourne exit 0 (collection passe)
    - `docker compose exec -T embedder pytest -m "not integration_ml" tests/test_remove_background.py 2>&1 | grep -E "(xfailed|passed)"` → 12 xfailed
    - `docker compose exec -T api vendor/bin/phpunit --filter RemoveBackgroundHandlerTest 2>&1 | grep -q "skipped"` → 3 skipped
  </acceptance_criteria>
  <done>12 stubs Python xfail + 2 stubs Python integration_ml + 2 stubs /health xfail + 2 stubs PHPUnit skipped. Tous collectables, aucune ImportError.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 04-01)

| Boundary | Description |
|----------|-------------|
| Aucune | Plan d'infrastructure de test, aucune surface réseau ou input utilisateur introduite |

## STRIDE Threat Register

Plan d'infrastructure de tests — pas de menace nouvelle introduite. Les threats Phase 4 (T-04-01..08) sont traitées dans les plans 02/03/04/05 qui implémentent les surfaces concrètes.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W0-01 | Tampering | requirements.txt pin ORT 1.22.0 | mitigate | Pin verbatim + commentaire bug #26261 ; plan 04-02 vérifie au runtime que la version est bien 1.22.x |
</threat_model>

<verification>
- `docker compose exec -T embedder pytest --collect-only -q` → exit 0
- `docker compose exec -T embedder pytest -m "not integration_ml" tests/test_remove_background.py 2>&1` → 12 xfailed, 0 errors
- `docker compose exec -T embedder pytest -m integration_ml --collect-only tests/test_remove_background_e2e.py` → 2 tests collected
- `docker compose exec -T api vendor/bin/phpunit --filter "RemoveBackground"` → 6 tests, 6 skipped
- `grep -q "onnxruntime==1.22.0" embedder/requirements.txt`
- `ls embedder/tests/fixtures/product_*.{png,jpg} | wc -l` → 4
</verification>

<success_criteria>
- [ ] `onnxruntime==1.22.0` pinned dans `embedder/requirements.txt`
- [ ] `huggingface_hub>=0.27,<1` ajouté
- [ ] Marker `integration_ml` déclaré dans `pytest.ini`
- [ ] 4 fixtures images créées (2048.png, 3000.jpg, 4500.jpg, with_alpha.png)
- [ ] `conftest.py` expose `product_*` + `mock_birefnet_session` + `mock_isnet_session` + `client`
- [ ] 12 stubs xfail dans `test_remove_background.py` (= tous les comportements VALIDATION 04-XX-01..09 + concurrent + structured log)
- [ ] 2 stubs `integration_ml` dans `test_remove_background_e2e.py`
- [ ] 2 stubs xfail ajoutés à `test_health.py` (D-11)
- [ ] 6 stubs `markTestSkipped` côté PHPUnit (3 handler + 3 DTO)
- [ ] Collection pytest + phpunit passe sans ImportError
</success_criteria>

<output>
After completion, create `.planning/phases/04-birefnet-endpoint-remove-background-deploy-gate/04-01-SUMMARY.md`
</output>
