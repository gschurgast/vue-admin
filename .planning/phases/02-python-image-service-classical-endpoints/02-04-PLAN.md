---
phase: 02-python-image-service-classical-endpoints
plan: 04
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - embedder/routers/img_add_background.py
  - embedder/app.py
  - embedder/tests/test_add_background.py
must_haves:
  truths:
    - "POST /img/add-background type:color #FF0000 sur RGB → image inchangée (pas de transparence à combler)"
    - "POST /img/add-background type:color #FF0000 sur RGBA → fond rouge composité sous l'image, alpha appliqué comme masque"
    - "POST /img/add-background type:asset reçoit un SECOND champ multipart `background_image` ; pas de fetch réseau, pas de S3"
    - "POST /img/add-background type:asset SANS background_image → 422"
    - "POST /img/add-background avec une URL dans params → 422 (anti-SSRF par schema)"
    - "Le background_image est redimensionné aux dimensions de l'image source avant composite"
    - "L'embedder n'a aucune dépendance S3 — confirmé par absence de boto3 / S3 env vars"
  artifacts:
    - path: "embedder/routers/img_add_background.py"
      provides: "POST /img/add-background avec type:color + type:asset"
      contains: "background_image"
  key_links:
    - from: "embedder/routers/img_add_background.py"
      to: "decode_image (background_image)"
      via: "Le second champ multipart est aussi décodé via decode_image() → applique 50 MPx + EXIF + SVG-reject"
      pattern: "decode_image"
autonomous: true
requirements:
  - IMGSVC-05
  - IMGSVC-09
---

<objective>
Implémenter `POST /img/add-background` avec les deux types : `color` (couleur unie #RRGGBB) et `asset` (image de fond fournie en SECOND champ multipart `background_image`). Le PHP de Phase 3 résoudra l'asset par son ID, lira ses bytes via Flysystem, et les enverra inline — l'embedder ne fait **aucun** I/O réseau et n'a aucun credential S3 (SSRF-safe par construction).

Purpose: C'est l'endpoint structurant de la phase. Le contrat multipart double-champ est documenté ici pour que la Phase 3 puisse l'implémenter côté PHP sans ambiguïté. Le schéma Pydantic interdit toute URL — anti-SSRF par type system.

Output: 1 router + 1 fichier de tests RED→GREEN couvrant color, asset, et tous les cas d'erreur.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-python-image-service-classical-endpoints/02-RESEARCH.md
@.planning/phases/02-python-image-service-classical-endpoints/02-01-PLAN.md
@.planning/REQUIREMENTS.md
@embedder/core/image_utils.py

<interfaces>
<!-- Contract that Phase 3 (PHP orchestrator) will call: -->

```
POST /img/add-background
Content-Type: multipart/form-data

Champs :
  - image (required, binary)       — l'image à laquelle ajouter un fond
  - params (required, JSON string) — { "type": "color", "color": "#RRGGBB" }
                                     OU { "type": "asset", "assetId": <int> }
  - background_image (optionnel)   — REQUIS si params.type == "asset"
                                     Bytes de l'asset fond, résolus par PHP via Flysystem.
                                     NE PAS envoyer pour type=color.

Réponse :
  - 200 + image/{png|jpeg|webp} (format préservé de l'image source)
  - 422 si type=asset sans background_image
  - 422 si params contient un champ URL (anti-SSRF par schema)
```

```python
# Schéma Pydantic — type:asset ne contient JAMAIS d'URL :
class BgColor(BaseModel):
    type: Literal["color"]
    color: str  # "#RRGGBB"

class BgAsset(BaseModel):
    type: Literal["asset"]
    assetId: int  # traçabilité log uniquement — pas utilisé pour fetch
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD /img/add-background type:color — composite sous RGBA</name>
  <files>embedder/routers/img_add_background.py, embedder/tests/test_add_background.py, embedder/app.py</files>
  <behavior>
    - `{type:"color", color:"#FF0000"}` sur 100x100 RGBA (alpha=128 vert) → résultat 100x100, pixel central est un mix de vert (avec alpha) sur rouge
    - `{type:"color", color:"#FFFFFF"}` sur RGBA totalement transparent → résultat 100% blanc
    - `{type:"color", color:"#FF0000"}` sur RGB → résultat inchangé (RGB n'a pas d'alpha à combler — implémentation choisie : on retourne l'image inchangée OU on composite, à fixer ; nous choisissons : RGB sans alpha → image source inchangée, le client n'a rien à composer)
    - Couleur hex invalide (#GGGGGG, "red", "##FFF") → 422
    - Type inconnu (`{type:"url", ...}`) → 422
    - Pas de `type` dans params → 422
    - SVG comme image principale → 422
  </behavior>
  <action>
    1. Create `embedder/routers/img_add_background.py`:
       ```python
       """POST /img/add-background — composite an image over a background.

       Two modes:
         - type=color: solid color (#RRGGBB) — single multipart field 'image' + params
         - type=asset: background bytes provided as SECOND multipart field 'background_image'.
           The PHP orchestrator (Phase 3) reads bytes from Flysystem and sends them inline.
           The 'assetId' in params is for log traceability ONLY — never used for fetch.
           NO URL is ever accepted in the schema — anti-SSRF by construction.
       """
       from __future__ import annotations
       import json
       from typing import Literal, Optional, Union
       from fastapi import APIRouter, File, Form, HTTPException, UploadFile
       from pydantic import BaseModel, Field, ValidationError
       from PIL import Image
       from core.image_utils import decode_image, image_response

       router = APIRouter()


       class BgColor(BaseModel):
           type: Literal["color"]
           color: str = Field(pattern=r"^#[0-9A-Fa-f]{6}$")


       class BgAsset(BaseModel):
           type: Literal["asset"]
           assetId: int = Field(gt=0)


       def _parse_hex(s: str) -> tuple[int, int, int]:
           s = s.lstrip("#")
           return (int(s[0:2], 16), int(s[2:4], 16), int(s[4:6], 16))


       def _composite_color(img: Image.Image, color_hex: str) -> Image.Image:
           """Place img over solid color. If img has no alpha, return as-is."""
           if img.mode != "RGBA":
               # No transparency to fill — return source untouched
               return img
           color = _parse_hex(color_hex)
           bg = Image.new("RGB", img.size, color)
           bg.paste(img, mask=img.split()[3])
           return bg


       def _composite_asset(img: Image.Image, bg_img: Image.Image) -> Image.Image:
           """Resize bg_img to match img.size, paste img on top using alpha if present."""
           bg = bg_img.resize(img.size, Image.Resampling.LANCZOS).convert("RGB")
           if img.mode == "RGBA":
               bg.paste(img, mask=img.split()[3])
           else:
               bg.paste(img)
           return bg


       @router.post("/img/add-background")
       async def add_background(
           image: UploadFile = File(...),
           params: str = Form(...),
           background_image: Optional[UploadFile] = File(None),
       ):
           try:
               p_raw = json.loads(params)
           except json.JSONDecodeError as exc:
               raise HTTPException(status_code=422, detail=f"Invalid JSON params: {exc}")

           if not isinstance(p_raw, dict) or "type" not in p_raw:
               raise HTTPException(status_code=422, detail="Missing 'type' in params.")

           # Defensive: reject any URL-like field even at top level (anti-SSRF deep-defense)
           for forbidden in ("url", "src", "href"):
               if forbidden in p_raw:
                   raise HTTPException(
                       status_code=422,
                       detail=f"Field '{forbidden}' is not allowed (anti-SSRF).",
                   )

           t = p_raw["type"]
           raw = await image.read()
           img = decode_image(raw)

           if t == "color":
               try:
                   pc = BgColor.model_validate(p_raw)
               except ValidationError as exc:
                   raise HTTPException(status_code=422, detail=exc.errors())
               result = _composite_color(img, pc.color)
           elif t == "asset":
               try:
                   pa = BgAsset.model_validate(p_raw)
               except ValidationError as exc:
                   raise HTTPException(status_code=422, detail=exc.errors())
               if background_image is None:
                   raise HTTPException(
                       status_code=422,
                       detail="background_image multipart field required for type=asset.",
                   )
               bg_raw = await background_image.read()
               bg_img = decode_image(bg_raw)
               # assetId is logged for traceability but never used for any I/O
               import logging
               logging.getLogger("embedder").info(
                   "add_background type=asset assetId=%d bg_size=%s img_size=%s",
                   pa.assetId, bg_img.size, img.size,
               )
               result = _composite_asset(img, bg_img)
           else:
               raise HTTPException(status_code=422, detail=f"Unknown type: {t}")

           fmt = (img.format or "PNG").lower()
           return image_response(result, fmt)
       ```
    2. Register router in `embedder/app.py`:
       ```python
       from routers import img_resize, img_crop, img_rotate, img_format_convert, img_add_background
       app.include_router(img_add_background.router)
       ```
    3. Create `embedder/tests/test_add_background.py` (color cases — asset cases in Task 2):
       ```python
       import io, json
       from PIL import Image


       def _post(client, image_bytes, params, mime="image/png", bg_bytes=None, bg_mime="image/png"):
           data = {"params": json.dumps(params)}
           files = {"image": ("t.png", image_bytes, mime)}
           if bg_bytes is not None:
               files["background_image"] = ("bg.png", bg_bytes, bg_mime)
           return client.post("/img/add-background", data=data, files=files)


       def _rgba(size=(100, 100), color=(0, 255, 0, 128)) -> bytes:
           img = Image.new("RGBA", size, color)
           buf = io.BytesIO(); img.save(buf, "PNG"); return buf.getvalue()


       def test_color_red_under_rgba(client):
           src = _rgba((50, 50), color=(0, 255, 0, 128))
           r = _post(client, src, {"type": "color", "color": "#FF0000"})
           assert r.status_code == 200
           out = Image.open(io.BytesIO(r.content)).convert("RGB")
           # Result should be a blend of green (alpha=128) over red — neither pure red nor pure green
           px = out.getpixel((25, 25))
           assert px[0] > 0 and px[1] > 0, f"expected red+green blend, got {px}"
           # No alpha channel remaining
           assert out.mode == "RGB"


       def test_color_white_under_fully_transparent(client):
           img = Image.new("RGBA", (50, 50), (0, 0, 0, 0))
           buf = io.BytesIO(); img.save(buf, "PNG"); src = buf.getvalue()
           r = _post(client, src, {"type": "color", "color": "#FFFFFF"})
           assert r.status_code == 200
           out = Image.open(io.BytesIO(r.content)).convert("RGB")
           assert out.getpixel((25, 25)) == (255, 255, 255)


       def test_color_on_rgb_unchanged(client, rgb_image):
           r = _post(client, rgb_image, {"type": "color", "color": "#000000"})
           assert r.status_code == 200
           out = Image.open(io.BytesIO(r.content))
           # Source is already opaque RGB → returned unchanged
           src = Image.open(io.BytesIO(rgb_image))
           assert out.size == src.size


       def test_invalid_hex_color_422(client, rgba_image):
           r = _post(client, rgba_image, {"type": "color", "color": "#GGGGGG"})
           assert r.status_code == 422


       def test_unknown_type_422(client, rgba_image):
           r = _post(client, rgba_image, {"type": "gradient"})
           assert r.status_code == 422


       def test_missing_type_422(client, rgba_image):
           r = _post(client, rgba_image, {"color": "#FF0000"})
           assert r.status_code == 422


       def test_url_field_rejected_ssrf(client, rgba_image):
           """Anti-SSRF deep defense: even outside type=color, any url field is rejected."""
           r = _post(client, rgba_image, {"type": "color", "color": "#FF0000", "url": "http://internal"})
           assert r.status_code == 422


       def test_addbg_rejects_svg(client, svg_bytes):
           r = _post(client, svg_bytes, {"type": "color", "color": "#FFFFFF"}, mime="image/svg+xml")
           assert r.status_code == 422
       ```
  </action>
  <verify>
    <automated>docker compose build embedder &amp;&amp; docker compose up -d embedder &amp;&amp; sleep 3 &amp;&amp; docker compose exec -T embedder pytest tests/test_add_background.py -x -v 2>&amp;1 | tail -30</automated>
  </verify>
  <done>8 tests verts pour les cas type:color + cas d'erreur génériques.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD /img/add-background type:asset — contrat multipart double-champ</name>
  <files>embedder/tests/test_add_background.py</files>
  <behavior>
    - type:asset + assetId:42 + background_image (bytes JPEG bleu 200x200) sur image RGBA 100x100 verte → résultat 100x100, fond bleu visible, image verte composée dessus
    - type:asset SANS background_image multipart → 422 + message clair
    - type:asset avec background_image SVG → 422 (decode_image rejette le SVG du fond)
    - type:asset avec background_image > 50 MPx → 422
    - type:asset avec assetId=0 ou négatif → 422 (Field(gt=0))
    - Le fond est redimensionné aux dimensions de l'image source (vérifier .size du résultat)
  </behavior>
  <action>
    Append to `embedder/tests/test_add_background.py`:
       ```python
       def test_asset_multipart_blue_bg_under_green(client):
           # Source: 100x100 green RGBA semi-transparent
           src_img = Image.new("RGBA", (100, 100), (0, 255, 0, 200))
           buf = io.BytesIO(); src_img.save(buf, "PNG"); src = buf.getvalue()

           # Background: 200x200 solid blue JPEG (will be resized to 100x100)
           bg_img = Image.new("RGB", (200, 200), (0, 0, 255))
           bbuf = io.BytesIO(); bg_img.save(bbuf, "JPEG"); bg = bbuf.getvalue()

           r = _post(client, src, {"type": "asset", "assetId": 42}, bg_bytes=bg, bg_mime="image/jpeg")
           assert r.status_code == 200, f"got {r.status_code}: {r.text[:200]}"
           out = Image.open(io.BytesIO(r.content)).convert("RGB")
           assert out.size == (100, 100)
           # Center pixel: composite of green-over-blue → has blue component but not pure blue
           px = out.getpixel((50, 50))
           assert px[2] > 0, f"expected blue contribution, got {px}"


       def test_asset_missing_background_image_422(client, rgba_image):
           r = _post(client, rgba_image, {"type": "asset", "assetId": 42})
           assert r.status_code == 422
           assert "background_image" in r.text


       def test_asset_invalid_assetid_422(client, rgba_image):
           bg = Image.new("RGB", (50, 50), (0, 0, 0))
           bbuf = io.BytesIO(); bg.save(bbuf, "PNG"); bg_bytes = bbuf.getvalue()
           r = _post(client, rgba_image, {"type": "asset", "assetId": 0}, bg_bytes=bg_bytes)
           assert r.status_code == 422


       def test_asset_svg_background_rejected(client, rgba_image, svg_bytes):
           r = _post(
               client, rgba_image,
               {"type": "asset", "assetId": 42},
               bg_bytes=svg_bytes, bg_mime="image/svg+xml",
           )
           assert r.status_code == 422


       def test_asset_huge_background_rejected(client, rgba_image, huge_image_bytes):
           r = _post(
               client, rgba_image,
               {"type": "asset", "assetId": 42},
               bg_bytes=huge_image_bytes,
           )
           assert r.status_code == 422
       ```
    Run: `docker compose exec -T embedder pytest tests/test_add_background.py -x -v`
  </action>
  <verify>
    <automated>docker compose exec -T embedder pytest tests/test_add_background.py -x -v 2>&amp;1 | tail -35</automated>
  </verify>
  <done>13 tests verts au total dans test_add_background.py (8 du Task 1 + 5 du Task 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /img/add-background | Multipart image + params + (optionnel) background_image |
| embedder → réseau extérieur | **AUCUNE** — le service n'a pas de boto3, pas de S3 env vars, pas de fetch HTTP sortant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | E (Elevation/SSRF) | type:asset fetch logic | mitigate | **Conception** : aucun champ URL accepté dans le schéma Pydantic. Le fond arrive en multipart inline depuis PHP (qui contrôle Flysystem). `BgAsset.assetId` est utilisé uniquement pour log. Test `test_url_field_rejected_ssrf`. |
| T-02-04-02 | E (SSRF deep defense) | params parsing | mitigate | Rejet explicite des clés `url`/`src`/`href` même si elles n'existent pas dans le schéma — defense in depth. |
| T-02-04-03 | D (DoS) | background_image potentiellement énorme | mitigate | `decode_image(bg_raw)` applique la même politique 50 MPx que pour l'image principale. Test `test_asset_huge_background_rejected`. |
| T-02-04-04 | T | background_image en SVG | mitigate | `decode_image` rejette SVG. Test `test_asset_svg_background_rejected`. |
| T-02-04-05 | T | assetId négatif/zéro | mitigate | `Field(gt=0)` → 422. Test `test_asset_invalid_assetid_422`. |
| T-02-04-06 | I | Log de assetId | accept | Log interne, ID public/numérique sans PII. |
</threat_model>

<verification>
## VERIFICATION

```bash
docker compose build embedder
docker compose up -d embedder
sleep 5

docker compose exec -T embedder pytest tests/test_add_background.py -v

# Confirmer absence de credentials S3 dans le container
docker compose exec -T embedder sh -c 'env | grep -iE "s3|aws|boto" || echo "OK: no S3/AWS env vars"'
docker compose exec -T embedder pip list 2>/dev/null | grep -iE "boto|s3" || echo "OK: no boto3/s3 lib"

# Smoke E2E type:color
python3 -c "from PIL import Image; i=Image.new('RGBA',(80,80),(0,255,0,128)); i.save('/tmp/rgba.png')"
curl -sS -X POST http://localhost:8000/img/add-background \
  -F 'params={"type":"color","color":"#FF0000"}' \
  -F 'image=@/tmp/rgba.png' \
  -o /tmp/r.png
python3 -c "from PIL import Image; i=Image.open('/tmp/r.png'); print('size=',i.size,'mode=',i.mode)"

# Smoke E2E type:asset (multipart 2 fields)
python3 -c "from PIL import Image; Image.new('RGB',(160,160),(0,0,255)).save('/tmp/bg.jpg')"
curl -sS -X POST http://localhost:8000/img/add-background \
  -F 'params={"type":"asset","assetId":42}' \
  -F 'image=@/tmp/rgba.png' \
  -F 'background_image=@/tmp/bg.jpg' \
  -o /tmp/r2.png
python3 -c "from PIL import Image; i=Image.open('/tmp/r2.png'); print('size=',i.size,'mode=',i.mode); print('pixel(40,40)=',i.convert('RGB').getpixel((40,40)))"
```
</verification>

<success_criteria>
- 13 tests verts dans test_add_background.py
- Aucun credential S3 dans le container (vérifié par grep env)
- Aucune lib boto3/s3 installée (vérifié par pip list)
- Contrat multipart double-champ documenté et testé pour Phase 3
- IMGSVC-05 + IMGSVC-09 couverts ; SSRF mitigé par schema
</success_criteria>

<output>
After completion, create `.planning/phases/02-python-image-service-classical-endpoints/02-04-SUMMARY.md`
</output>
