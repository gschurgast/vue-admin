---
phase: 02-python-image-service-classical-endpoints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - embedder/requirements-dev.txt
  - embedder/pytest.ini
  - embedder/Dockerfile
  - embedder/requirements.txt
  - embedder/app.py
  - embedder/core/__init__.py
  - embedder/core/image_utils.py
  - embedder/tests/__init__.py
  - embedder/tests/conftest.py
  - embedder/tests/test_decode_image.py
  - embedder/tests/test_health.py
autonomous: true
requirements:
  - IMGSVC-08
  - IMGSVC-10
must_haves:
  truths:
    - "pytest + httpx + pytest-asyncio sont installables dans le container embedder"
    - "Une commande `docker compose exec embedder pytest tests/ -x` exécute la suite verte"
    - "Le helper `decode_image()` rejette les SVG (422), les binaires non-image (422), les images > 50 MPx (422)"
    - "Le helper applique `ImageOps.exif_transpose` (réassigné) avant `.load()`"
    - "GET /health retourne `{status, models: {clip, birefnet, stable_diffusion}}` avec un statut par modèle"
    - "L'endpoint /embed existant continue de fonctionner sans changement de signature"
  artifacts:
    - path: "embedder/requirements-dev.txt"
      provides: "Dépendances de test (pytest, pytest-asyncio, httpx)"
      contains: "pytest"
    - path: "embedder/pytest.ini"
      provides: "Config pytest avec asyncio_mode=auto"
    - path: "embedder/core/image_utils.py"
      provides: "decode_image(), content_type_for_format(), image_response()"
      exports: ["decode_image", "image_response", "content_type_for_format", "MAX_PIXELS"]
    - path: "embedder/tests/conftest.py"
      provides: "Fixtures partagées (client, rgb_image, rgba_image, exif_rot6_jpeg, huge_image_mock)"
    - path: "embedder/app.py"
      provides: "Health endpoint refactorisé + import pillow_avif au module level"
      contains: "import pillow_avif"
  key_links:
    - from: "embedder/core/image_utils.py"
      to: "PIL.ImageOps.exif_transpose"
      via: "import"
      pattern: "ImageOps\\.exif_transpose"
    - from: "embedder/tests/conftest.py"
      to: "fastapi.testclient.TestClient"
      via: "import"
      pattern: "TestClient\\(app\\)"
---

<objective>
Wave 0 — Mettre en place l'infrastructure de tests (pytest + httpx + pytest-asyncio), extraire le helper `decode_image()` partagé (50 MPx guard + exif_transpose + reject SVG), installer `pillow-avif-plugin`, et refactoriser `/health` vers le schéma multi-modèles.

Purpose: Sans Wave 0, aucun des 4 plans suivants ne peut écrire de tests RED. Le helper `decode_image()` est consommé par les 5 endpoints à venir — il doit exister, être testé, et appliquer la politique de sécurité (IMGSVC-08) une seule fois.

Output: requirements-dev.txt + pytest.ini + tests/conftest.py + core/image_utils.py + /health refacto + tests verts pour decode_image et /health.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-python-image-service-classical-endpoints/02-RESEARCH.md
@embedder/app.py
@embedder/Dockerfile
@embedder/requirements.txt
@CLAUDE.md

<interfaces>
<!-- Contrats que ce plan crée et que les plans 02-02..02-05 consommeront. -->

```python
# embedder/core/image_utils.py
from PIL import Image
from fastapi import Response

MAX_PIXELS: int = 50_000_000

def decode_image(raw: bytes) -> Image.Image:
    """Open lazy, check size before .load(), exif_transpose, load.
    Raises HTTPException(422) on SVG/non-image/oversized."""

def content_type_for_format(fmt: str) -> str:
    """Maps 'png'|'jpg'|'jpeg'|'webp'|'avif' to MIME type. 'jpg' -> 'image/jpeg'."""

def image_response(img: Image.Image, fmt: str, quality: int | None = None,
                   extra_headers: dict | None = None) -> Response:
    """Encode img in fmt, return FastAPI Response with X-Processing-Time header."""
```

```python
# embedder/app.py — health refactor
@app.get("/health")
def health() -> dict:
    return {
        "status": "ok",
        "models": {
            "clip": {"status": "loaded"|"lazy"|"failed", "name": MODEL_NAME, "dim": 512},
            "birefnet": {"status": "not_loaded"},
            "stable_diffusion": {"status": "not_loaded"},
        },
    }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dev deps + AVIF plugin + pytest config</name>
  <files>embedder/requirements-dev.txt, embedder/requirements.txt, embedder/pytest.ini, embedder/Dockerfile, embedder/tests/__init__.py, embedder/tests/conftest.py</files>
  <behavior>
    - `docker compose build embedder` succeeds with new deps
    - `docker compose exec embedder python -c "import pillow_avif; from PIL import Image; import io; img = Image.new('RGB', (10,10)); buf = io.BytesIO(); img.save(buf, 'AVIF'); print(len(buf.getvalue()))"` prints > 0
    - `docker compose exec embedder pytest --version` prints `pytest 9.x`
    - `docker compose exec embedder python -c "import httpx, pytest_asyncio; print('ok')"` prints `ok`
  </behavior>
  <action>
    1. Create `embedder/requirements-dev.txt`:
       ```
       pytest==9.0.3
       pytest-asyncio==1.4.0
       httpx==0.28.1
       ```
    2. Append to `embedder/requirements.txt`:
       ```
       pillow-avif-plugin==1.5.5
       ```
    3. Create `embedder/pytest.ini`:
       ```ini
       [pytest]
       asyncio_mode = auto
       testpaths = tests
       ```
    4. Modify `embedder/Dockerfile` to install dev deps:
       After the existing `RUN pip install --no-cache-dir -r requirements.txt`, add:
       ```dockerfile
       COPY requirements-dev.txt .
       RUN pip install --no-cache-dir -r requirements-dev.txt
       ```
       Also add `COPY tests/ ./tests/` after `COPY app.py .` (will be a no-op until tests/ exists).
    5. Create empty `embedder/tests/__init__.py`.
    6. Create `embedder/tests/conftest.py` with shared fixtures:
       ```python
       import io
       import pytest
       from PIL import Image
       from fastapi.testclient import TestClient
       from app import app

       @pytest.fixture
       def client():
           return TestClient(app)

       @pytest.fixture
       def rgb_image() -> bytes:
           img = Image.new("RGB", (200, 150), color=(255, 0, 0))
           buf = io.BytesIO(); img.save(buf, "PNG"); return buf.getvalue()

       @pytest.fixture
       def rgba_image() -> bytes:
           img = Image.new("RGBA", (200, 200), color=(0, 255, 0, 128))
           buf = io.BytesIO(); img.save(buf, "PNG"); return buf.getvalue()

       @pytest.fixture
       def jpeg_image() -> bytes:
           img = Image.new("RGB", (300, 200), color=(0, 0, 255))
           buf = io.BytesIO(); img.save(buf, "JPEG", quality=90); return buf.getvalue()

       @pytest.fixture
       def webp_image() -> bytes:
           img = Image.new("RGB", (150, 150), color=(128, 128, 128))
           buf = io.BytesIO(); img.save(buf, "WEBP"); return buf.getvalue()

       @pytest.fixture
       def exif_rot6_jpeg() -> bytes:
           """JPEG 100x200 (portrait) with EXIF Orientation=6 (must be rotated 90°CW on display).
           After exif_transpose, the image should be 200x100 landscape."""
           import struct
           img = Image.new("RGB", (100, 200), color=(255, 255, 0))
           buf = io.BytesIO()
           # Build EXIF with Orientation=6 using piexif-like manual bytes via Pillow's exif
           exif = img.getexif()
           exif[0x0112] = 6  # Orientation tag
           img.save(buf, "JPEG", exif=exif.tobytes())
           return buf.getvalue()

       @pytest.fixture
       def svg_bytes() -> bytes:
           return b'<?xml version="1.0"?><svg xmlns="http://www.w3.org/2000/svg" width="10" height="10"/>'

       @pytest.fixture
       def huge_image_bytes() -> bytes:
           """Generate a PNG with declared dimensions > 50 MPx without actually allocating pixels.
           We allocate a small image but craft a PNG with manipulated IHDR. Simpler: build a real
           but big PNG of low color depth so it stays under memory; pixel count w*h must exceed 50e6."""
           img = Image.new("L", (8000, 7000), color=128)  # 56 MPx, 1 byte/px = 56 MB - acceptable for test
           buf = io.BytesIO(); img.save(buf, "PNG", compress_level=1); return buf.getvalue()

       @pytest.fixture
       def corrupt_bytes() -> bytes:
           return b"this is not an image, just garbage bytes \x00\x01\x02"
       ```
  </action>
  <verify>
    <automated>docker compose build embedder &amp;&amp; docker compose up -d embedder &amp;&amp; docker compose exec -T embedder pytest --collect-only tests/ 2>&amp;1 | head -20</automated>
  </verify>
  <done>
    - `docker compose build embedder` succeeds.
    - `docker compose exec embedder pytest --version` returns 9.x.
    - conftest.py loads without ImportError when pytest collects.
    - pillow_avif importable + can encode AVIF in container.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create core/image_utils.py with decode_image + write tests RED→GREEN</name>
  <files>embedder/core/__init__.py, embedder/core/image_utils.py, embedder/tests/test_decode_image.py</files>
  <behavior>
    - decode_image(rgb PNG bytes) returns Image with size (200, 150)
    - decode_image(svg bytes) raises HTTPException 422 with "image" in detail
    - decode_image(corrupt bytes) raises HTTPException 422
    - decode_image(huge 8000x7000 PNG) raises HTTPException 422 with "50 MPx" or "trop grande" in detail
    - decode_image(exif_rot6_jpeg) returns image whose .size is (200, 100) — EXIF applied (input was 100x200 portrait, rotated 90° → 200x100 landscape)
    - content_type_for_format("jpg") == "image/jpeg"
    - content_type_for_format("jpeg") == "image/jpeg"
    - content_type_for_format("png") == "image/png"
    - content_type_for_format("webp") == "image/webp"
    - content_type_for_format("avif") == "image/avif"
    - image_response(img, "png") returns Response with media_type=image/png + X-Processing-Time header set
  </behavior>
  <action>
    1. Create `embedder/core/__init__.py` (empty).
    2. Create `embedder/core/image_utils.py`:
       ```python
       """Shared image helpers — decode, encode, MIME mapping.

       SECURITY (per IMGSVC-08):
       - Image.open() is LAZY: .size is available BEFORE pixel decoding.
       - We check w*h > 50 MPx FIRST, then exif_transpose (which RETURNS a new Image —
         we MUST reassign), then .load() to actually materialize pixels.
       - SVG is rejected (raster endpoints only).
       - We DO NOT touch Image.MAX_IMAGE_PIXELS globally because /embed uses Pillow too.
       """
       from __future__ import annotations

       import io
       import time
       from typing import Optional

       from fastapi import HTTPException, Response
       from PIL import Image, ImageOps, UnidentifiedImageError

       MAX_PIXELS: int = 50_000_000  # 50 megapixels

       CONTENT_TYPES = {
           "png": "image/png",
           "jpg": "image/jpeg",
           "jpeg": "image/jpeg",
           "webp": "image/webp",
           "avif": "image/avif",
       }


       def _looks_like_svg(raw: bytes) -> bool:
           head = raw.lstrip()[:512].lower()
           return b"<svg" in head or (head.startswith(b"<?xml") and b"<svg" in head)


       def decode_image(raw: bytes) -> Image.Image:
           if not raw:
               raise HTTPException(status_code=422, detail="Empty image payload.")
           if _looks_like_svg(raw):
               raise HTTPException(
                   status_code=422,
                   detail="SVG non supporté par les endpoints de transformation raster.",
               )
           try:
               img = Image.open(io.BytesIO(raw))
           except UnidentifiedImageError as exc:
               raise HTTPException(status_code=422, detail=f"Fichier image non reconnu : {exc}")
           except Exception as exc:  # pragma: no cover - defensive
               raise HTTPException(status_code=422, detail=f"Image illisible : {exc}")

           # .size is populated by open() WITHOUT decoding pixels
           w, h = img.size
           if w * h > MAX_PIXELS:
               raise HTTPException(
                   status_code=422,
                   detail=f"Image trop grande ({w}x{h} = {w * h / 1e6:.1f} MPx). Maximum : 50 MPx.",
               )

           # exif_transpose returns a NEW Image — must reassign
           img = ImageOps.exif_transpose(img)
           img.load()
           return img


       def content_type_for_format(fmt: str) -> str:
           return CONTENT_TYPES.get(fmt.lower(), "application/octet-stream")


       def image_response(
           img: Image.Image,
           fmt: str,
           quality: Optional[int] = None,
           extra_headers: Optional[dict] = None,
       ) -> Response:
           t0 = time.perf_counter()
           buf = io.BytesIO()
           save_kwargs: dict = {}
           if quality is not None:
               save_kwargs["quality"] = quality
           pillow_fmt = fmt.upper().replace("JPG", "JPEG")
           img.save(buf, format=pillow_fmt, **save_kwargs)
           elapsed_ms = (time.perf_counter() - t0) * 1000
           headers = {"X-Processing-Time": f"{elapsed_ms:.1f}ms"}
           if extra_headers:
               headers.update(extra_headers)
           return Response(
               content=buf.getvalue(),
               media_type=content_type_for_format(fmt),
               headers=headers,
           )
       ```
    3. Create `embedder/tests/test_decode_image.py`:
       ```python
       import pytest
       from fastapi import HTTPException
       from core.image_utils import decode_image, content_type_for_format, image_response


       def test_decode_rgb_png(rgb_image):
           img = decode_image(rgb_image)
           assert img.size == (200, 150)
           assert img.mode in ("RGB", "RGBA")


       def test_decode_rejects_svg(svg_bytes):
           with pytest.raises(HTTPException) as exc:
               decode_image(svg_bytes)
           assert exc.value.status_code == 422
           assert "SVG" in exc.value.detail


       def test_decode_rejects_corrupt(corrupt_bytes):
           with pytest.raises(HTTPException) as exc:
               decode_image(corrupt_bytes)
           assert exc.value.status_code == 422


       def test_decode_rejects_empty():
           with pytest.raises(HTTPException) as exc:
               decode_image(b"")
           assert exc.value.status_code == 422


       def test_decode_rejects_huge(huge_image_bytes):
           with pytest.raises(HTTPException) as exc:
               decode_image(huge_image_bytes)
           assert exc.value.status_code == 422
           assert "MPx" in exc.value.detail or "trop grande" in exc.value.detail


       def test_decode_applies_exif_orientation_6(exif_rot6_jpeg):
           img = decode_image(exif_rot6_jpeg)
           # Source was 100x200 portrait with Orientation=6 (rotate 90° CW)
           # After exif_transpose → 200x100 landscape
           assert img.size == (200, 100), f"expected (200,100) after EXIF transpose, got {img.size}"


       def test_content_type_jpg_normalised_to_jpeg():
           assert content_type_for_format("jpg") == "image/jpeg"
           assert content_type_for_format("jpeg") == "image/jpeg"
           assert content_type_for_format("png") == "image/png"
           assert content_type_for_format("webp") == "image/webp"
           assert content_type_for_format("avif") == "image/avif"


       def test_image_response_sets_processing_time_header(rgb_image):
           img = decode_image(rgb_image)
           resp = image_response(img, "png")
           assert resp.media_type == "image/png"
           assert "x-processing-time" in {k.lower() for k in resp.headers.keys()}
           assert resp.body  # non-empty bytes
       ```
  </action>
  <verify>
    <automated>docker compose exec -T embedder pytest tests/test_decode_image.py -x -v 2>&amp;1 | tail -25</automated>
  </verify>
  <done>
    - 8 tests verts dans test_decode_image.py.
    - SVG, corrupt, empty, huge tous rejetés en 422.
    - EXIF Orientation=6 redresse correctement (100x200 → 200x100).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor /health to new schema + import pillow_avif at module level</name>
  <files>embedder/app.py, embedder/tests/test_health.py</files>
  <behavior>
    - GET /health returns 200 with `{status: "ok", models: {clip, birefnet, stable_diffusion}}`
    - models.clip has keys `status`, `name`, `dim`
    - models.clip.status is "loaded" or "lazy" (one of the enum values)
    - models.birefnet.status == "not_loaded"
    - models.stable_diffusion.status == "not_loaded"
    - /embed endpoint still accepts a multipart file and returns 200 with `embedding` array of 512 floats
    - `pillow_avif` is imported at module level so AVIF codec is registered globally
  </behavior>
  <action>
    1. Modify `embedder/app.py`:
       - At top of imports (after `from __future__`), add:
         ```python
         import pillow_avif  # noqa: F401  — registers AVIF codec via import side-effect
         ```
       - Replace the existing `health()` function with the new schema-aware version:
         ```python
         from enum import Enum

         class ModelStatus(str, Enum):
             loaded = "loaded"
             lazy = "lazy"
             not_loaded = "not_loaded"
             failed = "failed"


         @app.get("/health")
         def health() -> dict:
             clip_status = ModelStatus.loaded if _model is not None else ModelStatus.lazy
             return {
                 "status": "ok",
                 "models": {
                     "clip": {
                         "status": clip_status.value,
                         "name": MODEL_NAME,
                         "dim": EMBEDDING_DIM,
                     },
                     "birefnet": {"status": ModelStatus.not_loaded.value},
                     "stable_diffusion": {"status": ModelStatus.not_loaded.value},
                 },
             }
         ```
       - Keep `_warmup`, `_embed_image`, `/embed` UNCHANGED.
    2. Create `embedder/tests/test_health.py`:
       ```python
       def test_health_schema(client):
           r = client.get("/health")
           assert r.status_code == 200
           body = r.json()
           assert body["status"] == "ok"
           assert "models" in body
           m = body["models"]
           assert set(m.keys()) >= {"clip", "birefnet", "stable_diffusion"}
           # clip details
           assert m["clip"]["status"] in {"loaded", "lazy"}
           assert m["clip"]["dim"] == 512
           assert "name" in m["clip"]
           # birefnet + sd not loaded in phase 2
           assert m["birefnet"]["status"] == "not_loaded"
           assert m["stable_diffusion"]["status"] == "not_loaded"


       def test_embed_endpoint_still_works(client, rgb_image):
           """Smoke test: /embed must not be broken by the refactor."""
           r = client.post("/embed", files={"file": ("test.png", rgb_image, "image/png")})
           assert r.status_code == 200
           body = r.json()
           assert "embedding" in body
           assert len(body["embedding"]) == 512


       def test_avif_codec_registered():
           """import pillow_avif at module load must register AVIF codec."""
           from PIL import Image
           import io
           img = Image.new("RGB", (16, 16), color=(0, 0, 0))
           buf = io.BytesIO()
           img.save(buf, "AVIF")  # would raise if codec not registered
           assert buf.tell() > 0
       ```
    3. After editing, rebuild embedder so the new app.py + pillow_avif are loaded:
       `docker compose build embedder && docker compose up -d embedder`
  </action>
  <verify>
    <automated>docker compose build embedder &amp;&amp; docker compose up -d embedder &amp;&amp; sleep 3 &amp;&amp; docker compose exec -T embedder pytest tests/test_health.py tests/test_decode_image.py -x -v 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - 3 tests verts dans test_health.py.
    - /embed continue de fonctionner (regression check vert).
    - AVIF codec enregistré (test_avif_codec_registered vert).
    - Full Wave 0 suite verte.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → embedder | Bytes multipart non fiables (peuvent être SVG, image bombe, binaire arbitraire) |
| embedder → /tmp / filesystem | N/A — tout en mémoire (BytesIO), aucun chemin construit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | D (DoS) | `decode_image()` | mitigate | Check `w * h > MAX_PIXELS (50_000_000)` AVANT `.load()` via `Image.open()` lazy. Test `test_decode_rejects_huge` verifie. |
| T-02-01-02 | S (Spoofing) | `decode_image()` SVG handling | mitigate | Pre-check `_looks_like_svg(raw)` AVANT Pillow `Image.open()` car certains SVG passent les magic bytes Pillow puis crash. Test `test_decode_rejects_svg` verifie. |
| T-02-01-03 | T (Tampering) | `decode_image()` corrupt input | mitigate | `UnidentifiedImageError` → 422 contrôlé, jamais 500. Test `test_decode_rejects_corrupt` verifie. |
| T-02-01-04 | I (Info disclosure) | `/health` endpoint | accept | Expose noms modèles + dims. Acceptable car endpoint interne (port 8000 jamais exposé publiquement, voir docker-compose.yml). |
| T-02-01-05 | E (Elevation) | `Image.MAX_IMAGE_PIXELS` global override | mitigate | NE PAS toucher `Image.MAX_IMAGE_PIXELS` globalement — check manuel `w*h > MAX_PIXELS` qui n'affecte pas `/embed` CLIP. |
</threat_model>

<verification>
## VERIFICATION

```bash
# Build + start
docker compose build embedder
docker compose up -d embedder
sleep 5

# Wave 0 full suite
docker compose exec -T embedder pytest tests/ -x -v

# Health endpoint smoke
curl -s http://localhost:8000/health | python -m json.tool
# Expect: { "status": "ok", "models": { "clip": {...}, "birefnet": {"status": "not_loaded"}, ... } }

# AVIF codec registered
docker compose exec -T embedder python -c "import pillow_avif; from PIL import Image; import io; img=Image.new('RGB',(8,8)); buf=io.BytesIO(); img.save(buf,'AVIF'); print('AVIF OK', len(buf.getvalue()), 'bytes')"

# /embed regression
curl -s -X POST -F "file=@mon_test.png" http://localhost:8000/embed | python -c "import json,sys; d=json.load(sys.stdin); print('dim=', len(d['embedding']))"
# Expect: dim= 512
```
</verification>

<success_criteria>
- requirements-dev.txt + pytest.ini + Dockerfile dev-deps installés
- `pillow-avif-plugin` importable et AVIF encode fonctionne
- `core/image_utils.py` expose `decode_image`, `content_type_for_format`, `image_response`, `MAX_PIXELS`
- 11 tests verts (test_decode_image: 8, test_health: 3)
- /embed CLIP endpoint inchangé fonctionnellement (régression vérifiée)
</success_criteria>

<output>
After completion, create `.planning/phases/02-python-image-service-classical-endpoints/02-01-SUMMARY.md`
</output>
