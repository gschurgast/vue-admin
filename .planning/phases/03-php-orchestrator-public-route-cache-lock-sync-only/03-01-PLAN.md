---
phase: 03-php-orchestrator-public-route-cache-lock-sync-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/Entity/AssetTransformation/AssetTransformation.php
  - api/src/Service/AssetTransformation/StepParams/ResizeStepParams.php
  - api/src/Service/AssetTransformation/StepParams/CropStepParams.php
  - api/src/Service/AssetTransformation/StepParams/RotateStepParams.php
  - api/src/Service/AssetTransformation/StepParams/FormatConvertStepParams.php
  - api/src/Service/AssetTransformation/StepParams/AddBackgroundStepParams.php
  - api/src/Service/AssetTransformation/StepParams/StepParamsFactory.php
  - api/src/EventListener/TransformationStepValidationListener.php
  - api/src/EventListener/TransformationHashListener.php
  - api/migrations/Version20260527000001.php
  - api/tests/Service/AssetTransformation/StepParams/StepParamsFactoryTest.php
autonomous: true
requirements:
  - HANDLERS-03
  - HANDLERS-05
must_haves:
  truths:
    - "Persister un TransformationStep avec params invalides lève ValidationFailedException → 422"
    - "Une transformation dont la dernière step n'est pas format_convert et dont le code contient '.jpg' ou '.jpeg' en cible reçoit un warning persisté 'alpha-flatten-on-jpeg'"
    - "Chaque type de step (resize/crop/rotate/format_convert/add_background) résolu par StepParamsFactory retourne le DTO PHP correct avec Assert appliqué"
  artifacts:
    - path: "api/src/Service/AssetTransformation/StepParams/StepParamsFactory.php"
      provides: "Hydrate + valide les params par StepType"
    - path: "api/src/EventListener/TransformationStepValidationListener.php"
      provides: "Doctrine prePersist/preUpdate qui valide via StepParamsFactory"
    - path: "api/migrations/Version20260527000001.php"
      provides: "Migration ajoutant warnings JSONB DEFAULT '[]' sur asset_transformation"
  key_links:
    - from: "TransformationStepValidationListener::prePersist"
      to: "StepParamsFactory::fromStep"
      via: "Doctrine LifecycleEventArgs"
      pattern: "StepParamsFactory::fromStep"
    - from: "TransformationHashListener"
      to: "AssetTransformation::setWarnings"
      via: "onFlush computeChangeSet"
      pattern: "setWarnings"
---

<objective>
Mettre en place les DTO Validators par step type (D-14, D-15), le hook Doctrine de validation (D-16), la persistance de la colonne `warnings` (D-18) et la dérivation du warning `alpha-flatten-on-jpeg` (HANDLERS-05) recalculée par le `TransformationHashListener` existant. Aucune dépendance HTTP/runtime ; bloc validation + schéma.

Purpose: Garantir que toute écriture (API, fixtures, console) d'un `TransformationStep` est validée selon son type, et que les warnings sont disponibles pour l'éditeur PWA (Phase 7) et le header runtime `X-Transformation-Warnings` (Plan 03).
Output: 5 DTO StepParams readonly, factory, listener Doctrine, migration `warnings JSONB`, extension du `TransformationHashListener`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-php-orchestrator-public-route-cache-lock-sync-only/03-CONTEXT.md
@api/src/Entity/AssetTransformation/AssetTransformation.php
@api/src/Entity/AssetTransformation/TransformationStep.php
@api/src/Enum/StepType.php
@api/src/EventListener/TransformationHashListener.php
@CLAUDE.md

<interfaces>
StepType cases : `resize | crop | rotate | format_convert | add_background | remove_background`.
TransformationStep::$params est un `array` (JSON) déjà persisté.
TransformationStep::$type retourne `StepType`.
AssetTransformation a déjà `versionHash` (string) ; on ajoute `warnings` (array → JSON).
Le TransformationHashListener actuel calcule `versionHash` dans `onFlush + postFlush` ; on insère le calcul `warnings` au même endroit.

Embedder DTO contracts (Phase 2 RESEARCH — source de vérité pour les Assert) :
- resize : `{ width?: int 1-8192, height?: int 1-8192, mode: 'fit'|'cover'|'contain', upscale?: bool }` ; au moins width OU height requis.
- crop : `{ x: int >=0, y: int >=0, width: int >=1, height: int >=1 }` OU `{ aspectRatio: string regex '^\d+:\d+$', anchor?: 'center'|'top'|'bottom'|'left'|'right' }`
- rotate : `{ angle: int -360..360, background?: string regex '^#[0-9a-fA-F]{6}$' }`
- format_convert : `{ format: 'png'|'jpg'|'jpeg'|'webp'|'avif', quality?: int 1-100 }`
- add_background : `{ type: 'color', color: '^#[0-9a-fA-F]{6}$' }` OU `{ type: 'asset', assetId: int >=1 }`. Aucune URL acceptée (SSRF-safe). En Phase 3, `type: 'ai_prompt'` rejeté à la validation (sera autorisé Phase 6).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StepParams DTOs + Factory + tests golden</name>
  <files>api/src/Service/AssetTransformation/StepParams/ResizeStepParams.php, api/src/Service/AssetTransformation/StepParams/CropStepParams.php, api/src/Service/AssetTransformation/StepParams/RotateStepParams.php, api/src/Service/AssetTransformation/StepParams/FormatConvertStepParams.php, api/src/Service/AssetTransformation/StepParams/AddBackgroundStepParams.php, api/src/Service/AssetTransformation/StepParams/StepParamsFactory.php, api/tests/Service/AssetTransformation/StepParams/StepParamsFactoryTest.php</files>
  <read_first>
    - api/src/Service/AssetTransformation/TransformationStorageKey.php
    - api/src/Entity/AssetTransformation/TransformationStep.php
    - api/src/Enum/StepType.php
    - api/src/ApiResource/ChatRequest.php (pattern DTO readonly + Assert)
    - .planning/phases/02-python-image-service-classical-endpoints/02-RESEARCH.md
    - .planning/phases/03-php-orchestrator-public-route-cache-lock-sync-only/03-CONTEXT.md (D-14, D-15)
  </read_first>
  <behavior>
    - Test 1 : `StepParamsFactory::fromStep` sur un TransformationStep de type RESIZE avec `{width: 800, mode: 'fit'}` retourne une instance `ResizeStepParams` ; validation passe.
    - Test 2 : ResizeStepParams sans width ni height → 1 violation Assert (NotBlank composite).
    - Test 3 : RotateStepParams angle=720 → violation Range.
    - Test 4 : FormatConvertStepParams format='gif' → violation Choice.
    - Test 5 : AddBackgroundStepParams `{type: 'asset', assetId: 12}` valide ; `{type: 'asset', url: 'http://x'}` rejeté (extra fields strict) ; `{type: 'ai_prompt', ...}` rejeté en Phase 3 (Choice limité à color|asset).
    - Test 6 : CropStepParams accepte les DEUX formes (xy-wh OU aspectRatio) ; `{x:0,y:0,width:0,height:0}` rejeté (width/height ≥ 1).
    - Test 7 : `StepParamsFactory::fromStep` sur type non géré (REMOVE_BACKGROUND en Phase 3) → throw `LogicException` ou `UnsupportedStepTypeException` explicite (sera étendu Phase 4).
  </behavior>
  <action>
Créer 5 classes `readonly` (public properties + Assert constraints) dans `api/src/Service/AssetTransformation/StepParams/`. Chaque classe a un constructeur `__construct(public readonly ...)`.

Asserts exacts (per D-14, dérivés des contrats Phase 2) :

**ResizeStepParams** :
```php
public function __construct(
    #[Assert\Range(min: 1, max: 8192)] public readonly ?int $width = null,
    #[Assert\Range(min: 1, max: 8192)] public readonly ?int $height = null,
    #[Assert\Choice(['fit','cover','contain'])] public readonly string $mode = 'fit',
    public readonly bool $upscale = false,
) {}
#[Assert\Callback]
public function validateAtLeastOneDim(ExecutionContextInterface $ctx): void {
    if ($this->width === null && $this->height === null) {
        $ctx->buildViolation('width or height required')->atPath('width')->addViolation();
    }
}
```

**CropStepParams** : 2 formes mutuellement exclusives via `#[Assert\Callback]` (exactement l'un des deux jeux : xy-wh OU aspectRatio).
- xy-wh : `x ≥ 0`, `y ≥ 0`, `width ≥ 1`, `height ≥ 1` (tous int).
- aspect : `aspectRatio` Regex `^\d+:\d+$`, `anchor` Choice `['center','top','bottom','left','right']`.

**RotateStepParams** : `angle: int Range(-360,360)`, `background: ?string Regex '/^#[0-9a-fA-F]{6}$/'`.

**FormatConvertStepParams** : `format: string Choice(['png','jpg','jpeg','webp','avif'])`, `quality: ?int Range(1,100)`.

**AddBackgroundStepParams** : `type: Choice(['color','asset'])` (Phase 3 — pas `ai_prompt`, ajouté Phase 6). Callback :
- type=color → color requis Regex hex.
- type=asset → assetId requis int ≥ 1. **PAS de propriété `url`**. Tout champ non listé doit causer une erreur via le Serializer en mode strict (`AbstractObjectNormalizer::ALLOW_EXTRA_ATTRIBUTES=false`).

**StepParamsFactory** : `final class StepParamsFactory` avec ctor injection `private SerializerInterface $serializer, private ValidatorInterface $validator`.
```php
public function fromStep(TransformationStep $step): object {
    $class = match($step->getType()) {
        StepType::RESIZE => ResizeStepParams::class,
        StepType::CROP => CropStepParams::class,
        StepType::ROTATE => RotateStepParams::class,
        StepType::FORMAT_CONVERT => FormatConvertStepParams::class,
        StepType::ADD_BACKGROUND => AddBackgroundStepParams::class,
        default => throw new UnsupportedStepTypeException(/* type non supporté en Phase 3 */),
    };
    $dto = $this->serializer->denormalize($step->getParams() ?? [], $class, 'array', [
        AbstractObjectNormalizer::ALLOW_EXTRA_ATTRIBUTES => false,
    ]);
    $violations = $this->validator->validate($dto);
    if (count($violations) > 0) {
        throw new ValidationFailedException($dto, $violations);
    }
    return $dto;
}
```

Créer aussi `api/src/Service/AssetTransformation/StepParams/UnsupportedStepTypeException.php` extends `\RuntimeException`.

Tests PHPUnit dans `tests/Service/AssetTransformation/StepParams/StepParamsFactoryTest.php` couvrant les 7 cas (utiliser `static::getContainer()` style WebTestCase pour récupérer SerializerInterface+ValidatorInterface).
  </action>
  <verify>
    <automated>docker compose exec api ./vendor/bin/phpunit tests/Service/AssetTransformation/StepParams/ --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "readonly" api/src/Service/AssetTransformation/StepParams/*.php | wc -l` >= 5
    - `grep -c "ALLOW_EXTRA_ATTRIBUTES.*false" api/src/Service/AssetTransformation/StepParams/StepParamsFactory.php` == 1
    - `phpunit tests/Service/AssetTransformation/StepParams/` : 7 tests passent
    - `grep -E "case 'ai_prompt'|'ai_prompt'" api/src/Service/AssetTransformation/StepParams/AddBackgroundStepParams.php` retourne vide (pas accepté en Phase 3)
  </acceptance_criteria>
  <done>5 DTOs + Factory + Exception créés, 7 tests passent, validation strict-fields active.</done>
</task>

<task type="auto">
  <name>Task 2: Migration warnings JSONB + entité + extension TransformationHashListener (alpha-flatten-on-jpeg) + Doctrine listener de validation</name>
  <files>api/migrations/Version20260527000001.php, api/src/Entity/AssetTransformation/AssetTransformation.php, api/src/EventListener/TransformationHashListener.php, api/src/EventListener/TransformationStepValidationListener.php</files>
  <read_first>
    - api/src/Entity/AssetTransformation/AssetTransformation.php
    - api/src/EventListener/TransformationHashListener.php
    - api/src/Service/AssetTransformation/StepParams/StepParamsFactory.php (créé Task 1)
    - api/migrations/ (dernière migration pour pattern)
    - .planning/phases/03-php-orchestrator-public-route-cache-lock-sync-only/03-CONTEXT.md (D-16, D-18)
  </read_first>
  <action>
**Migration** `Version20260527000001.php` :
```php
public function up(Schema $schema): void {
    $this->addSql("ALTER TABLE asset_transformation ADD COLUMN warnings JSONB NOT NULL DEFAULT '[]'");
}
public function down(Schema $schema): void {
    $this->addSql("ALTER TABLE asset_transformation DROP COLUMN warnings");
}
```

**Entité** `AssetTransformation.php` :
Ajouter (groupe lecture `asset_transformation:read` aligné sur les autres) :
```php
#[ORM\Column(type: Types::JSON, options: ['default' => '[]'])]
#[Groups(['asset_transformation:read'])]
private array $warnings = [];

public function getWarnings(): array { return $this->warnings; }
public function setWarnings(array $warnings): static { $this->warnings = $warnings; return $this; }
```
(Vérifier le nom exact du groupe existant via `grep "Groups" api/src/Entity/AssetTransformation/AssetTransformation.php` et l'utiliser tel quel.)

**TransformationHashListener** :
Lors du recalcul `versionHash`, dériver les warnings :
```php
private function computeWarnings(AssetTransformation $tx): array {
    $warnings = [];
    $steps = $tx->getSteps()->toArray();
    // Tri par sortOrder/position (vérifier la propriété d'ordre existante)
    usort($steps, fn($a,$b) => $a->getSortOrder() <=> $b->getSortOrder());
    $hasFormatConvertJpeg = false;
    $lastFormat = null;
    foreach ($steps as $s) {
        if ($s->getType() === StepType::FORMAT_CONVERT) {
            $fmt = $s->getParams()['format'] ?? null;
            $lastFormat = $fmt;
            if (in_array($fmt, ['jpg','jpeg'], true)) {
                $hasFormatConvertJpeg = true;
            }
        }
    }
    // Heuristique HANDLERS-05 : si format cible JPEG SANS add_background dans la chaîne
    if ($hasFormatConvertJpeg) {
        $hasAddBg = false;
        foreach ($steps as $s) {
            if ($s->getType() === StepType::ADD_BACKGROUND) { $hasAddBg = true; break; }
        }
        if (!$hasAddBg) {
            $warnings[] = ['code' => 'alpha-flatten-on-jpeg', 'stepIndex' => null];
        }
    }
    return $warnings;
}
```
Appeler `$tx->setWarnings($this->computeWarnings($tx))` au même endroit que `setVersionHash`. Inclure dans `computeChangeSet` du UoW si `onFlush`.

**Listener de validation** `TransformationStepValidationListener.php` (D-16) :
```php
#[AsDoctrineListener(event: Events::prePersist, priority: 0, connection: 'default')]
#[AsDoctrineListener(event: Events::preUpdate, priority: 0, connection: 'default')]
final class TransformationStepValidationListener {
    public function __construct(private StepParamsFactory $factory) {}
    public function prePersist(LifecycleEventArgs $args): void { $this->validate($args); }
    public function preUpdate(PreUpdateEventArgs $args): void { $this->validate($args); }
    private function validate(LifecycleEventArgs $args): void {
        $entity = $args->getObject();
        if (!$entity instanceof TransformationStep) return;
        $this->factory->fromStep($entity); // throws ValidationFailedException
    }
}
```
API Platform convertit `ValidationFailedException` en 422 automatiquement (déjà câblé). Pour la console/fixtures, l'exception remonte telle quelle.
  </action>
  <verify>
    <automated>docker compose exec api ./vendor/bin/phpunit tests/Service/AssetTransformation/ --testdox && docker compose exec api php bin/console doctrine:schema:validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "warnings JSONB" api/migrations/Version20260527000001.php` >= 1
    - `grep -c "private array \$warnings" api/src/Entity/AssetTransformation/AssetTransformation.php` == 1
    - `grep -c "alpha-flatten-on-jpeg" api/src/EventListener/TransformationHashListener.php` == 1
    - `grep -c "AsDoctrineListener" api/src/EventListener/TransformationStepValidationListener.php` >= 2
    - `doctrine:schema:validate` retourne "schema in sync" après migration appliquée (Task 3)
  </acceptance_criteria>
  <done>Migration + entité + listener hash étendu + listener validation créés.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply Doctrine migration + integration test alpha-flatten warning</name>
  <files>api/tests/Integration/AssetTransformation/WarningsDerivationTest.php</files>
  <read_first>
    - api/src/EventListener/TransformationHashListener.php (extended Task 2)
    - api/migrations/Version20260527000001.php
  </read_first>
  <action>
1. Appliquer la migration : `docker compose exec api php bin/console doctrine:migrations:migrate -n`
2. Vérifier le schéma : `docker compose exec api php bin/console doctrine:schema:validate`
3. Sur le schéma de test (si SQLite/pgsql séparé) : `docker compose exec api php bin/console doctrine:schema:update --force --env=test`
4. Écrire un test d'intégration (Kernel test) :
   - Créer une `AssetTransformation` avec 2 steps : `RESIZE {width:800}` puis `FORMAT_CONVERT {format:'jpg'}`
   - Persister + flush
   - Recharger depuis la DB → `getWarnings()` contient `[['code'=>'alpha-flatten-on-jpeg','stepIndex'=>null]]`
   - Cas inverse : ajouter `ADD_BACKGROUND {type:'color',color:'#FFFFFF'}` entre les deux → `getWarnings()` est `[]`
   - Cas inverse : `FORMAT_CONVERT {format:'webp'}` au lieu de jpg → `getWarnings()` est `[]`
5. Test additionnel D-16 : tenter de persister un `TransformationStep` `RESIZE` avec `params: ['mode' => 'invalid']` → attendre `ValidationFailedException`.
  </action>
  <verify>
    <automated>docker compose exec api php bin/console doctrine:migrations:migrate -n && docker compose exec api ./vendor/bin/phpunit tests/Integration/AssetTransformation/WarningsDerivationTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose exec api php bin/console doctrine:schema:validate` retourne "schema is in sync"
    - `docker compose exec api psql ... -c "\d asset_transformation" | grep warnings` montre `jsonb` NOT NULL DEFAULT `'[]'::jsonb` (ou équivalent via SQL `SELECT column_default FROM information_schema.columns WHERE table_name='asset_transformation' AND column_name='warnings'`)
    - WarningsDerivationTest : 4 cas verts (jpg→warning, jpg+addBg→pas de warning, webp→pas de warning, params invalides→422)
  </acceptance_criteria>
  <done>Migration appliquée, schéma sync, warnings dérivés correctement, validation par listener active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Platform POST/PATCH → Doctrine persist | Données externes (admin authentifié) → entité Doctrine |
| Doctrine fixtures / console → Entity | Code interne mais bypasse souvent les Validators API Platform |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T (Tampering) | AddBackgroundStepParams | mitigate | `ALLOW_EXTRA_ATTRIBUTES=false` dans Serializer denormalize → rejet de toute clé inconnue (ex: `url`) ; SSRF impossible par construction |
| T-03-02 | E (Elevation) | StepParamsFactory | mitigate | Liste blanche `match` sur StepType ; `default` lève `UnsupportedStepTypeException` — pas de fallback permissif |
| T-03-03 | I (Information disclosure) | warnings column | accept | JSON public OK ; aucun PII ; visible uniquement aux admins via groupes de lecture existants |
| T-03-04 | T (Tampering) | TransformationStepValidationListener | mitigate | Hook Doctrine `prePersist + preUpdate` couvre toutes les écritures (API + console + fixtures) — pas de bypass possible sans désactiver le listener |
| T-03-05 | D (DoS) | Validator (Regex / JSON denormalize) | accept | Payloads bornés par `TransformationStep::$params` (déjà colonne JSON Postgres ≤ qq KB par step) ; pas de regex catastrophic backtracking dans nos patterns |
</threat_model>

<verification>
- 7 tests StepParamsFactoryTest passent
- 4 tests WarningsDerivationTest passent
- `doctrine:schema:validate` = OK
- `grep "Groups(\['asset_transformation:read'\])" api/src/Entity/AssetTransformation/AssetTransformation.php` montre `warnings` dans le groupe read
</verification>

<success_criteria>
SC4 (DTO Validators rejettent à la persistance + alpha-flatten warning visible) : couvert intégralement.
</success_criteria>

<output>
After completion, create `.planning/phases/03-php-orchestrator-public-route-cache-lock-sync-only/03-01-SUMMARY.md`
</output>
