---
phase: 01-domain-versioning-foundation
plan: 03
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - api/src/Validator/TransformationCode.php
  - api/src/Validator/TransformationCodeValidator.php
  - api/src/Entity/AssetTransformation/AssetTransformation.php
  - api/tests/Unit/Validator/TransformationCodeValidatorTest.php
autonomous: true
requirements:
  - TRANSFORM-01
  - TRANSFORM-06
user_setup: []

must_haves:
  truths:
    - "POST /api/asset_transformations avec code='hero-webp' (kebab-case valide) retourne 201"
    - "POST avec code='hero_webp' (snake_case) retourne 422 avec message d'erreur explicite"
    - "POST avec code='api' (réservé) retourne 422 avec message 'reserved'"
    - "POST avec code='a' (mono-caractère) retourne 422"
    - "POST avec code='-leading-hyphen' ou 'trailing-hyphen-' retourne 422"
    - "POST avec code='UPPER' retourne 422"
    - "Le constraint TransformationCode est attaché à la propriété code de AssetTransformation"
  artifacts:
    - path: "api/src/Validator/TransformationCode.php"
      provides: "Constraint avec message + reservedCodes[] + maxLength"
      contains: "class TransformationCode extends Constraint"
    - path: "api/src/Validator/TransformationCodeValidator.php"
      provides: "ConstraintValidator : regex kebab-case + blocklist + mono-char guard"
      contains: "class TransformationCodeValidator extends ConstraintValidator"
    - path: "api/tests/Unit/Validator/TransformationCodeValidatorTest.php"
      provides: "Cases valides + invalides + réservés + mono-char"
      min_lines: 60
  key_links:
    - from: "AssetTransformation.code"
      to: "TransformationCode constraint"
      via: "#[AppAssert\\TransformationCode]"
      pattern: "AppAssert\\\\TransformationCode"
    - from: "API Platform 422 response"
      to: "TransformationCodeValidator violations"
      via: "Symfony Validator pipeline"
      pattern: "hydra:Error|violations"
---

<objective>
Créer un constraint Symfony Validator dédié `AppAssert\TransformationCode` (kebab-case + blocklist), distinct de `AppAssert\Code` existant (snake_case, à NE PAS modifier — Pitfall E du RESEARCH). L'attacher à la propriété `code` de `AssetTransformation`. TDD strict : tests RED → validator GREEN.

Purpose: couvre TRANSFORM-01 (validation kebab-case à la création) et TRANSFORM-06 (blocklist `api`, `admin`, `t`, `_`, `assets`, mono-caractères). C'est la barrière de sécurité contre le code-squatting des routes futures (Phase 3 expose `/t/{code}/...`).

Output: 1 constraint + 1 validator + 1 test file + l'attribute posé sur `AssetTransformation.code`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@./CLAUDE.md
@.planning/phases/01-domain-versioning-foundation/01-RESEARCH.md
@.planning/phases/01-domain-versioning-foundation/01-01-SUMMARY.md
@api/src/Validator/Code.php
@api/src/Validator/CodeValidator.php
@api/src/Entity/AssetTransformation/AssetTransformation.php

<interfaces>
Pattern existant Code.php (snake_case) — à ne PAS modifier :
```php
namespace App\Validator;
#[\Attribute(\Attribute::TARGET_PROPERTY | \Attribute::TARGET_METHOD)]
class Code extends Constraint {
    public string $message = 'The code "{{ value }}" is invalid. ...';
    public int $maxLength = 50;
}
```

Le validator existant utilise le regex `/^[a-z]+(_[a-z]+)*$/` — incompatible avec kebab-case.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Tests du TransformationCodeValidator</name>
  <files>api/tests/Unit/Validator/TransformationCodeValidatorTest.php</files>
  <behavior>
    Codes VALIDES (doivent produire 0 violation) :
    - "hero-webp", "thumb-product", "p-1", "x-1-2-3", "abc-def-ghi" (50 chars max)

    Codes INVALIDES (doivent produire 1 violation message standard) :
    - "" (vide — accepté en l'absence de NotBlank, mais ici testons que le validator skip null/empty per le contrat existant)
    - "UPPER" (majuscules)
    - "hero_webp" (underscores)
    - "-leading" (commence par tiret)
    - "trailing-" (se termine par tiret)
    - "double--hyphen" (tirets consécutifs)
    - "with space"
    - chaîne de 51 chars (dépasse maxLength)
    - "a" (mono-caractère) — message standard

    Codes RÉSERVÉS (doivent produire 1 violation message "reserved") :
    - "api", "admin", "t", "_", "assets"
  </behavior>
  <action>
Créer `api/tests/Unit/Validator/TransformationCodeValidatorTest.php` étendant `Symfony\Component\Validator\Test\ConstraintValidatorTestCase` (helper fourni par symfony/validator pour tester les validators en isolation, sans container).

Squelette :
```php
namespace App\Tests\Unit\Validator;

use App\Validator\TransformationCode;
use App\Validator\TransformationCodeValidator;
use Symfony\Component\Validator\Test\ConstraintValidatorTestCase;

final class TransformationCodeValidatorTest extends ConstraintValidatorTestCase
{
    protected function createValidator(): TransformationCodeValidator
    {
        return new TransformationCodeValidator();
    }

    /** @dataProvider validCodes */
    public function testValidCodesProduceNoViolation(string $code): void {
        $this->validator->validate($code, new TransformationCode());
        $this->assertNoViolation();
    }
    public static function validCodes(): array {
        return [['hero-webp'], ['thumb-product'], ['p-1'], ['x-1-2-3'], ['abc']];
    }

    /** @dataProvider invalidCodes */
    public function testInvalidCodesProduceStandardViolation(string $code): void {
        $constraint = new TransformationCode();
        $this->validator->validate($code, $constraint);
        $this->buildViolation($constraint->message)
            ->setParameter('{{ value }}', '"'.$code.'"')
            ->assertRaised();
    }
    public static function invalidCodes(): array {
        return [
            ['UPPER'],
            ['hero_webp'],
            ['-leading'],
            ['trailing-'],
            ['double--hyphen'],
            ['with space'],
            [str_repeat('a-', 30)], // > 50 chars
            ['a'], // mono-char
        ];
    }

    /** @dataProvider reservedCodes */
    public function testReservedCodesProduceReservedViolation(string $code): void {
        $constraint = new TransformationCode();
        $this->validator->validate($code, $constraint);
        $this->buildViolation($constraint->reservedMessage)
            ->setParameter('{{ value }}', '"'.$code.'"')
            ->assertRaised();
    }
    public static function reservedCodes(): array {
        return [['api'], ['admin'], ['t'], ['_'], ['assets']];
    }

    public function testNullValueSkipped(): void {
        $this->validator->validate(null, new TransformationCode());
        $this->assertNoViolation();
    }
    public function testEmptyStringSkipped(): void {
        $this->validator->validate('', new TransformationCode());
        $this->assertNoViolation();
    }
}
```

**Note sur "a" (mono-char) vs "_" (réservé mono-char)** : "_" est dans `reservedCodes` → message `reservedMessage`. "a" passe d'abord le check de longueur (1) → message standard. Cohérent avec RESEARCH §Pattern 7.

**Note sur "t"** : "t" est dans `reservedCodes` mais est aussi mono-char. Le validator doit retourner le message **reserved** (plus précis) — la blocklist est checkée AVANT le mono-char. Adapter l'ordre dans le validator (Task 2) pour que la blocklist passe en premier.

Lancer pour confirmer RED :
```bash
docker compose exec api vendor/bin/phpunit --filter TransformationCodeValidator
```
Attendu : `Class "App\Validator\TransformationCode" not found`.
  </action>
  <verify>
    <automated>docker compose exec api vendor/bin/phpunit --filter TransformationCodeValidator 2>&1 | grep -E "not found|FAILURES|ERRORS"</automated>
  </verify>
  <done>Le test file existe avec tous les cas dataprovider. La suite échoue en RED ("Class not found").</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implémenter TransformationCode + TransformationCodeValidator</name>
  <files>api/src/Validator/TransformationCode.php, api/src/Validator/TransformationCodeValidator.php</files>
  <behavior>
    Tous les tests du Task 1 verts.
  </behavior>
  <action>
**Fichier 1 — `api/src/Validator/TransformationCode.php`** :

```php
<?php
namespace App\Validator;

use Symfony\Component\Validator\Constraint;

#[\Attribute(\Attribute::TARGET_PROPERTY | \Attribute::TARGET_METHOD)]
class TransformationCode extends Constraint
{
    public string $message = 'The code {{ value }} is invalid. It must be kebab-case (lowercase letters, digits, hyphens), start with a letter, no consecutive hyphens, between 2 and 50 chars.';
    public string $reservedMessage = 'The code {{ value }} is reserved and cannot be used.';

    /** @var list<string> */
    public array $reservedCodes = ['api', 'admin', 't', '_', 'assets'];

    public int $maxLength = 50;
}
```

**Fichier 2 — `api/src/Validator/TransformationCodeValidator.php`** :

```php
<?php
namespace App\Validator;

use Symfony\Component\Validator\Constraint;
use Symfony\Component\Validator\ConstraintValidator;
use Symfony\Component\Validator\Exception\UnexpectedTypeException;
use Symfony\Component\Validator\Exception\UnexpectedValueException;

class TransformationCodeValidator extends ConstraintValidator
{
    public function validate(mixed $value, Constraint $constraint): void
    {
        if (!$constraint instanceof TransformationCode) {
            throw new UnexpectedTypeException($constraint, TransformationCode::class);
        }
        if (null === $value || '' === $value) {
            return; // NotBlank est le responsable du "champ requis"
        }
        if (!is_string($value)) {
            throw new UnexpectedValueException($value, 'string');
        }

        // Blocklist FIRST (cas "t" mono-char doit retourner "reserved", pas le message standard)
        if (in_array($value, $constraint->reservedCodes, strict: true)) {
            $this->context->buildViolation($constraint->reservedMessage)
                ->setParameter('{{ value }}', $this->formatValue($value))
                ->addViolation();
            return;
        }

        // Length
        if (strlen($value) > $constraint->maxLength) {
            $this->addStandard($constraint, $value);
            return;
        }

        // Mono-caractère interdit (per TRANSFORM-06)
        if (strlen($value) === 1) {
            $this->addStandard($constraint, $value);
            return;
        }

        // Kebab-case : [a-z][a-z0-9]*(-[a-z0-9]+)*
        // → commence par une lettre, segments alphanum séparés par un seul tiret,
        //   pas de tirets consécutifs, pas de leading/trailing hyphen.
        if (!preg_match('/^[a-z][a-z0-9]*(-[a-z0-9]+)*$/', $value)) {
            $this->addStandard($constraint, $value);
        }
    }

    private function addStandard(TransformationCode $c, string $value): void
    {
        $this->context->buildViolation($c->message)
            ->setParameter('{{ value }}', $this->formatValue($value))
            ->addViolation();
    }
}
```

**Note ConstraintValidatorTestCase** : la méthode `formatValue()` de Symfony enveloppe par défaut la valeur dans `"..."`. Si les assertions de test échouent sur le formatage, ajuster soit le test (utiliser `$this->formatValue($code)` directement), soit reproduire le wrapping `'"'.$code.'"'`.

**Fichier 3 — Attacher le constraint à l'entité** :
Éditer `api/src/Entity/AssetTransformation/AssetTransformation.php` : ajouter `use App\Validator as AppAssert;` en haut puis l'attribut `#[AppAssert\TransformationCode]` sur la propriété `$code`, en plus de `#[Assert\NotBlank]` déjà présent. **Ne pas toucher `AppAssert\Code` ailleurs dans le codebase.**

Lancer :
```bash
docker compose exec api vendor/bin/phpunit --filter TransformationCodeValidator
```
Attendu : tous verts.

Smoke-test bout-en-bout via API :
```bash
# Cas réservé → 422
curl -s -X POST -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/ld+json' \
  -d '{"code":"api","label":"X","steps":[]}' \
  http://localhost:8080/api/asset_transformations -w '%{http_code}\n' | tail -1
# Attendu : 422
```
  </action>
  <verify>
    <automated>docker compose exec api vendor/bin/phpunit --filter TransformationCodeValidator 2>&1 | grep -E "OK \(.*tests"</automated>
  </verify>
  <done>Les 3 fichiers (constraint + validator + entity) sont à jour. La suite unit du validator est verte. POST avec code 'api' renvoie HTTP 422 via curl.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/asset_transformations | `code` est non-confiant ; passe par la chaîne Symfony Validator avant persistance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Spoofing | Code 'api' usurpe la route /api | mitigate | Blocklist explicite dans `TransformationCode::reservedCodes`. Le check passe AVANT la regex (ordre du validator) pour message clair. Renforcé en Phase 3 par route requirement regex |
| T-01-03-02 | Spoofing | Code 't' usurpe le préfixe public futur /t/ | mitigate | 't' présent dans `reservedCodes` |
| T-01-03-03 | Tampering | Bypass de la validation via PATCH | mitigate | Constraint posé au niveau **propriété** → s'applique à toutes les opérations API Platform (Post, Patch). Test E2E `PATCH /api/asset_transformations/{id} {code: 'api'}` doit aussi 422 (vérif manuelle) |
| T-01-03-04 | Info disclosure | Message d'erreur expose la blocklist | accept | La blocklist est publique de toute façon (routes /api, /t, /admin sont observables). Pas de fuite d'info sensible. |
| T-01-03-05 | DoS | ReDoS sur regex kebab-case | accept | Regex `^[a-z][a-z0-9]*(-[a-z0-9]+)*$` est linéaire (pas de backtracking catastrophique). Max 50 chars borné par check de longueur. Risque négligeable |
| T-01-03-06 | Elevation of privilege | Codes Unicode/punycode contournant la blocklist | mitigate | Regex ASCII-only `[a-z]` ; tout caractère non-ASCII est rejeté avant d'arriver à la blocklist |
</threat_model>

<verification>
```bash
# 1. Suite unit validator verte
docker compose exec api vendor/bin/phpunit --filter TransformationCodeValidator

# 2. Smoke-test API : codes valides acceptés (201)
TOKEN=$(curl -s -X POST http://localhost:8080/api/login -H 'Content-Type: application/json' -d '{"email":"admin@example.com","password":"admin"}' | jq -r .token)

for valid in hero-webp thumb-product p-1; do
  echo -n "$valid → "
  curl -s -o /dev/null -w '%{http_code}\n' -X POST -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/ld+json' \
    -d "{\"code\":\"$valid\",\"label\":\"L\",\"steps\":[]}" http://localhost:8080/api/asset_transformations
done
# Attendu : 201 pour chaque (puis cleanup)

# 3. Smoke-test API : codes invalides + réservés → 422
for invalid in api admin t _ assets hero_webp UPPER -bad bad- a; do
  echo -n "$invalid → "
  curl -s -o /dev/null -w '%{http_code}\n' -X POST -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/ld+json' \
    -d "{\"code\":\"$invalid\",\"label\":\"L\",\"steps\":[]}" http://localhost:8080/api/asset_transformations
done
# Attendu : 422 pour chaque

# 4. PATCH cas : changer un code existant vers 'api' → 422
# (à faire manuellement après avoir un id valide)
```
</verification>

<success_criteria>
- [ ] `TransformationCode.php` et `TransformationCodeValidator.php` créés dans `api/src/Validator/`
- [ ] `AppAssert\Code` existant **NON modifié** (AssetFlag, AttributeDefinition, Collection, Taxonomy doivent rester fonctionnels)
- [ ] L'entité `AssetTransformation` porte `#[AppAssert\TransformationCode]` sur sa propriété `$code`
- [ ] Tous les tests `TransformationCodeValidatorTest` verts
- [ ] POST avec un code de la blocklist retourne 422 (smoke-test curl)
- [ ] POST avec un code kebab-case valide retourne 201
- [ ] PATCH d'un code existant vers une valeur invalide retourne 422
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-versioning-foundation/01-03-SUMMARY.md` listing:
- Décision : pourquoi un nouveau constraint plutôt que d'étendre `Code` (Pitfall E)
- La regex finale + les codes réservés
- Ordre des checks (blocklist > length > mono-char > regex)
- Résultats du smoke-test (matrice valide/invalide/réservé)
- Note pour Plan 04 : la validation ayant lieu AVANT le listener onFlush, le listener n'aura jamais à gérer un code invalide
</output>

> **Rappel Webfacto** : la liste des codes réservés (`api`, `admin`, `t`, `_`, `assets`) est figée dans le code. Toute évolution (nouveau préfixe de route protégé) requiert un cadrage Webfacto pour éviter les collisions en prod.
