---
phase: 01-domain-versioning-foundation
plan: 05
type: tdd
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - api/src/Service/AssetTransformation/TransformationStorageKey.php
  - api/tests/Unit/Service/AssetTransformation/TransformationStorageKeyTest.php
  - pwa/src/config/AssetTransformation.json
autonomous: true
requirements:
  - TRANSFORM-01
  - TRANSFORM-05
user_setup: []

must_haves:
  truths:
    - "TransformationStorageKey::forVariant(42, 'abc12345defgh...', 1234, 'webp') retourne 'transformations/42-vabc12345/1/1234.webp'"
    - "L'extension avec point '.jpg' produit la même clé que sans point 'jpg'"
    - "Le shard est floor(assetId / 1000) — pattern identique à Asset::computeS3Key()"
    - "Le hash8 est substr(versionHash, 0, 8) — cohérent avec le log du PurgeHandler (Plan 04)"
    - "La PWA expose AssetTransformation dans son menu Settings avec colonnes code/label/versionHash et formulaire d'édition steps"
  artifacts:
    - path: "api/src/Service/AssetTransformation/TransformationStorageKey.php"
      provides: "Helper statique pur : forVariant(transformationId, versionHash, assetId, ext): string"
      contains: "final class TransformationStorageKey"
    - path: "api/tests/Unit/Service/AssetTransformation/TransformationStorageKeyTest.php"
      provides: "Cases : variant key, leading dot, shard 0/15, hash8 substring"
      min_lines: 40
    - path: "pwa/src/config/AssetTransformation.json"
      provides: "Config CRUD PWA : list (code, label, versionHash), edit (code, label, steps), show (tout)"
  key_links:
    - from: "TransformationStorageKey::forVariant"
      to: "Asset::computeS3Key() (pattern miroir)"
      via: "intdiv(assetId, 1000) shard convention"
      pattern: "intdiv\\(.*1000\\)"
    - from: "PWA Resource menu"
      to: "AssetTransformation API"
      via: "pwa/src/config/AssetTransformation.json"
      pattern: "AssetTransformation.json"
---

<objective>
Livrer la primitive `TransformationStorageKey` (helper statique pour la clé S3 des variants) — utilisée dès la Phase 3 par le route handler et la commande de purge. Ajouter la config PWA pour rendre la ressource éditable depuis l'interface admin (les composants par défaut suffisent, pas de custom needed en Phase 1).

Purpose: couvre TRANSFORM-05 (helper S3 key, miroir exact de `Asset::computeS3Key`) et TRANSFORM-01 (UI admin pour créer/lister depuis la PWA). C'est le wrap-up du milestone : l'admin peut maintenant gérer les transformations end-to-end (API + UI), et le préfixe S3 est défini pour les phases suivantes.

Output: 1 helper + 1 test file + 1 JSON config PWA + types TS régénérés à jour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@./CLAUDE.md
@.planning/phases/01-domain-versioning-foundation/01-RESEARCH.md
@.planning/phases/01-domain-versioning-foundation/01-01-SUMMARY.md
@api/src/Entity/Asset/Asset.php
@pwa/src/config/Asset.json
@pwa/src/config/AssetFlag.json

<interfaces>
Pattern Asset::computeS3Key() (à miroir) — extrait de api/src/Entity/Asset/Asset.php :
```php
public static function computeS3Key(int $id, string $extension): string {
    $shard = intdiv($id, 1000);
    return sprintf('%d/%d.%s', $shard, $id, ltrim($extension, '.'));
    // → "0/42.jpg", "15/15234.mp4"
}
```

Pattern PWA config (Asset.json — pas le composant custom mais la structure du schema-driven CRUD) :
```json
{
  "filters": { "fields": [{ "code": "" }, { "label": "" }] },
  "list":    { "fields": [{ "code": "" }, { "label": "" }, { "versionHash": "" }] },
  "edit":    { "fields": [{ "code": "" }, { "label": "" }, { "steps": "" }] },
  "show":    { "fields": [{ "code": "" }, { "label": "" }, { "versionHash": "" }, { "steps": "" }] }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Tests du TransformationStorageKey</name>
  <files>api/tests/Unit/Service/AssetTransformation/TransformationStorageKeyTest.php</files>
  <behavior>
    - Test 1 : forVariant(42, 'abc12345defghijklmnopqrst...', 1234, 'webp') === 'transformations/42-vabc12345/1/1234.webp'
    - Test 2 : extension '.jpg' (avec point) === 'jpg' (sans point)
    - Test 3 : assetId 500 → shard 0 ; assetId 1234 → shard 1 ; assetId 15234 → shard 15
    - Test 4 : hash8 est toujours les 8 premiers chars du versionHash fourni
    - Test 5 : hash de moins de 8 chars (cas dégradé) → utiliser substr complet sans crash
  </behavior>
  <action>
Créer `api/tests/Unit/Service/AssetTransformation/TransformationStorageKeyTest.php` :

```php
<?php
namespace App\Tests\Unit\Service\AssetTransformation;

use App\Service\AssetTransformation\TransformationStorageKey;
use PHPUnit\Framework\TestCase;

final class TransformationStorageKeyTest extends TestCase
{
    public function testStandardVariantKey(): void
    {
        $key = TransformationStorageKey::forVariant(
            transformationId: 42,
            versionHash: 'abc12345defghijklmnopqrstuvwxyz1234567890',  // 40 chars
            assetId: 1234,
            ext: 'webp',
        );
        self::assertSame('transformations/42-vabc12345/1/1234.webp', $key);
    }

    public function testExtensionWithLeadingDotIsStripped(): void
    {
        $a = TransformationStorageKey::forVariant(1, 'hash12345678901234567890123456789012abcd', 500, 'jpg');
        $b = TransformationStorageKey::forVariant(1, 'hash12345678901234567890123456789012abcd', 500, '.jpg');
        self::assertSame($a, $b);
        self::assertSame('transformations/1-vhash1234/0/500.jpg', $a);
    }

    /** @dataProvider shardCases */
    public function testShardComputation(int $assetId, int $expectedShard): void
    {
        $key = TransformationStorageKey::forVariant(1, str_repeat('a', 40), $assetId, 'png');
        self::assertSame(
            sprintf('transformations/1-v%s/%d/%d.png', str_repeat('a', 8), $expectedShard, $assetId),
            $key,
        );
    }
    public static function shardCases(): array {
        return [
            [0, 0], [1, 0], [999, 0],
            [1000, 1], [1234, 1], [1999, 1],
            [15234, 15],
            [1_000_000, 1000],
        ];
    }

    public function testHash8IsAlwaysFirst8Chars(): void
    {
        $key = TransformationStorageKey::forVariant(7, '0123456789abcdef0123456789abcdef01234567', 42, 'png');
        self::assertStringContainsString('-v01234567/', $key);
    }
}
```

Lancer pour RED :
```bash
docker compose exec api vendor/bin/phpunit --filter TransformationStorageKey
```
Attendu : `Class "App\Service\AssetTransformation\TransformationStorageKey" not found`.
  </action>
  <verify>
    <automated>docker compose exec api vendor/bin/phpunit --filter TransformationStorageKey 2>&1 | grep -E "not found|FAILURES|ERRORS"</automated>
  </verify>
  <done>Le test file existe avec ~4 méthodes + dataProvider shardCases. Suite échoue en RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implémenter TransformationStorageKey</name>
  <files>api/src/Service/AssetTransformation/TransformationStorageKey.php</files>
  <behavior>
    Tous les tests du Task 1 passent.
  </behavior>
  <action>
Créer `api/src/Service/AssetTransformation/TransformationStorageKey.php` exactement selon Pattern 5 du RESEARCH :

```php
<?php
namespace App\Service\AssetTransformation;

/**
 * Pure helper for transformation variant S3/Flysystem keys.
 *
 * Format: transformations/{transformationId}-v{hash8}/{shard}/{assetId}.{ext}
 *
 * - hash8 = substr($versionHash, 0, 8) — first 8 hex chars of the sha1 versionHash.
 *           Storage uses short hash for readability ; the full 40-char hash stays in DB
 *           for traceability and collision avoidance.
 * - shard = intdiv($assetId, 1000) — same convention as Asset::computeS3Key()
 *           (api/src/Entity/Asset/Asset.php) — DIVERGENCE = cache miss cluster-wide.
 * - ext   = leading dot stripped.
 *
 * Examples:
 *   forVariant(42,  'abc12345...', 1234,  'webp')  → 'transformations/42-vabc12345/1/1234.webp'
 *   forVariant(1,   'hash...',     500,   '.jpg')  → 'transformations/1-vhash.../0/500.jpg'
 *   forVariant(7,   'fff...',      15234, 'png')   → 'transformations/7-vfff.../15/15234.png'
 */
final class TransformationStorageKey
{
    public static function forVariant(
        int    $transformationId,
        string $versionHash,
        int    $assetId,
        string $ext,
    ): string {
        $shard = intdiv($assetId, 1000);
        $hash8 = substr($versionHash, 0, 8);
        $ext   = ltrim($ext, '.');

        return sprintf(
            'transformations/%d-v%s/%d/%d.%s',
            $transformationId,
            $hash8,
            $shard,
            $assetId,
            $ext,
        );
    }

    /**
     * Returns the directory prefix (no trailing slash) that contains all variants
     * for a given (transformationId, versionHash). Used by the purge handler in Phase 3+.
     */
    public static function prefixForVariants(int $transformationId, string $versionHash): string
    {
        return sprintf('transformations/%d-v%s', $transformationId, substr($versionHash, 0, 8));
    }
}
```

Méthode bonus `prefixForVariants` : utile dès Phase 3 pour la purge (le handler du Plan 04 logguera ce préfixe). Ajouter 1 test :
```php
public function testPrefixForVariants(): void {
    self::assertSame('transformations/42-vabc12345',
        TransformationStorageKey::prefixForVariants(42, 'abc12345xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'));
}
```

Lancer la suite — toute verte attendue :
```bash
docker compose exec api vendor/bin/phpunit
```
  </action>
  <verify>
    <automated>docker compose exec api vendor/bin/phpunit --filter TransformationStorageKey 2>&1 | grep -E "OK \(.*tests"</automated>
  </verify>
  <done>Le helper existe, statique, sans deps. Tous les tests + le bonus `prefixForVariants` verts. Suite complète PHPUnit verte (regroupe Plans 02, 03, 04, 05).</done>
</task>

<task type="auto">
  <name>Task 3: PWA — Config CRUD AssetTransformation + regen types</name>
  <files>pwa/src/config/AssetTransformation.json</files>
  <action>
Créer `pwa/src/config/AssetTransformation.json`. Pas besoin de composant custom — le schema-driven CRUD du projet (voir `pwa/src/config/Asset.json` et `AssetFlag.json` pour les patterns) gère par défaut les champs simples + les relations.

```json
{
  "filters": {
    "fields": [
      { "code": "" },
      { "label": "" }
    ]
  },
  "list": {
    "fields": [
      { "code": "" },
      { "label": "" },
      { "versionHash": "" }
    ]
  },
  "edit": {
    "fields": [
      { "code": "" },
      { "label": "" },
      { "steps": "" }
    ]
  },
  "show": {
    "fields": [
      { "code": "" },
      { "label": "" },
      { "versionHash": "" },
      { "steps": "" }
    ]
  }
}
```

**Note importante** : `versionHash` n'apparaît PAS dans `edit.fields` (cohérent avec le fait qu'il est readonly côté API — Plan 01 et Plan 04). Il apparaît en `list` et `show` pour traçabilité (utile pour identifier quelle version d'une transformation est en cache).

**Note sur `steps`** : le rendu par défaut affichera un champ générique pour la collection. Il est ATTENDU que ce rendu soit médiocre en Phase 1 — l'éditeur drag-and-drop optimisé (EDITOR-02, EDITOR-03) vient en Phase 7. La Phase 1 valide uniquement que la création/modification basique fonctionne via l'API.

Régénérer les types TS pour s'assurer qu'ils sont à jour après les Plans 01-04 :
```bash
make generate-types
```

Vérifier visuellement (manuel — voir verification block) :
- Ouvrir http://localhost:5173
- Login admin
- Le menu "Settings" doit lister "Asset Transformations"
- Cliquer → page de liste vide
- Cliquer "Créer" → formulaire avec code, label, steps
- Saisir `hero-webp` / `Hero WebP` / ajouter un step `resize` → save → 201
- Reload : versionHash apparaît dans la liste (40 chars)
- Cliquer édition → modifier le step → save → le versionHash change (visible en colonne)
- Cliquer delete → confirmation → row disparait → vérifier dans la queue Redis qu'un PurgeMessage a été dispatché
  </action>
  <verify>
    <automated>test -f pwa/src/config/AssetTransformation.json && grep -q '"steps"' pwa/src/config/AssetTransformation.json && grep -q "AssetTransformation" pwa/src/types/api.d.ts && echo OK</automated>
  </verify>
  <done>Le fichier JSON existe et expose code/label/steps en edit, plus versionHash en list/show. Les types TS sont à jour. La PWA expose la ressource dans le menu Settings et permet le CRUD basique (vérif manuelle).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TransformationStorageKey | Aucune — pure fonction de chaîne. Tous les inputs sont déjà confiants (int + string contrôlée côté serveur). |
| Config PWA → API | Pas de nouvelle surface — la config décrit le rendu CRUD générique déjà sécurisé (JWT + ROLE_ADMIN au niveau de l'API depuis le Plan 01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | Path traversal via extension | mitigate | `ltrim($ext, '.')` retire le point initial ; aucune validation de path. Mais l'extension n'est PAS fournie par le user en Phase 1 (extraite de l'URL `/t/{code}/{id}.{ext}` en Phase 3 et bornée par regex route requirement). Phase 1 = helper non exposé. Risque réel adressé en Phase 3. |
| T-01-05-02 | Tampering | TransformationId / assetId négatifs ou très grands | accept | Type-hint `int` côté PHP. Postgres SERIAL (int4) borne naturellement. Pas d'usage avec valeur user-fournie en Phase 1. |
| T-01-05-03 | Information disclosure | Le préfixe S3 contient l'id de transformation | accept | Les ids ne sont pas des secrets. Les URLs sont publiques de toute façon (Phase 3 expose /t/...). |
| T-01-05-04 | Information disclosure | versionHash de 8 chars suffisant pour collision ? | accept | hash8 = 4^8 = 65k préfixes. Avec ~1000 transformations actives × ~10 versions chacune = 10000 hashes. Probabilité collision ≈ 0.07% (anniversaire). Tolérable — collision = simple cache miss (recompute), pas de corruption. Documenté dans le RESEARCH §Open Questions Q1. |
| T-01-05-05 | Elevation of privilege | PWA config expose versionHash en édition | mitigate | versionHash absent de `edit.fields` ; sera de toute façon rejeté par l'API (group `:write` ne le contient pas — Plan 01) |
| T-01-05-06 | SSRF | Aucune | accept | Pas d'I/O en Phase 1 |
</threat_model>

<verification>
```bash
# 1. Helper testé en isolation
docker compose exec api vendor/bin/phpunit --filter TransformationStorageKey

# 2. Suite complète PHPUnit verte (rappel : couvre les 4 plans précédents aussi)
docker compose exec api vendor/bin/phpunit

# 3. Config PWA présente
test -f pwa/src/config/AssetTransformation.json && jq -r '.list.fields | length' pwa/src/config/AssetTransformation.json
# Attendu : 3

# 4. Types TS à jour
grep -c "AssetTransformation" pwa/src/types/api.d.ts
# Attendu : > 0

# 5. PWA build OK (compile sans erreur de type)
docker compose exec pwa npm run build 2>&1 | tail -20
# Attendu : pas d'erreur

# 6. Cohérence hash8 entre helper et handler de purge (smoke)
docker compose exec api php -r "
  require '/srv/api/vendor/autoload.php';
  \$h = 'abcdef0123456789abcdef0123456789abcdef01';
  echo App\Service\AssetTransformation\TransformationStorageKey::prefixForVariants(42, \$h);
"
# Attendu : transformations/42-vabcdef01

# 7. Smoke E2E PWA (manuel) — CRUD via UI : créer, éditer step, delete, voir versionHash changer
```
</verification>

<success_criteria>
- [ ] `TransformationStorageKey.php` créé, méthodes `forVariant()` + `prefixForVariants()` statiques
- [ ] Test file couvre variant key, leading dot, shard 0/1/15/1000, hash8
- [ ] `pwa/src/config/AssetTransformation.json` créé, `versionHash` absent de `edit`
- [ ] `pwa/src/types/api.d.ts` à jour (regen via `make generate-types`)
- [ ] `npm run build` PWA passe sans erreur
- [ ] Suite PHPUnit complète verte (couvre 01-02 + 01-03 + 01-04 + 01-05)
- [ ] E2E manuel : admin peut créer/éditer/supprimer une transformation via la PWA
- [ ] Toutes les success criteria globales de Phase 1 (ROADMAP §Phase 1) sont satisfaites
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-versioning-foundation/01-05-SUMMARY.md` listing:
- La signature finale de `forVariant(transformationId, versionHash, assetId, ext)` + exemple de clé générée
- La cohérence hash8 (8 premiers chars) avec le log du PurgeHandler — VÉRIFIÉE
- Config PWA livrée : seule la ressource est exposée, pas de composant custom (Phase 7 livrera l'éditeur drag-and-drop EDITOR-02/03)
- État final de la Phase 1 :
  - TRANSFORM-01 : créable avec code unique kebab-case ✓
  - TRANSFORM-02 : steps ordonnés typés ✓
  - TRANSFORM-03 : modifiable post-création ✓
  - TRANSFORM-04 : versionHash auto-recomputed ✓
  - TRANSFORM-05 : purge async dispatchée (handler no-op Phase 1, S3 réel Phase 3) ✓
  - TRANSFORM-06 : codes réservés rejetés 422 ✓
- Handoff pour Phase 2 (endpoints Python) : aucune dépendance directe — Phase 2 peut démarrer en parallèle après Phase 1 merged
- Handoff pour Phase 3 (route publique + cache) : utilisera `TransformationStorageKey::forVariant()` et `prefixForVariants()` ; complètera le handler `PurgeTransformationVariantsHandler` avec la vraie suppression Flysystem
</output>

> **Rappel Webfacto** : la convention de clé S3 `transformations/{id}-v{hash8}/{shard}/{assetId}.{ext}` est désormais figée et alimente l'estimation de coût de stockage prod. À cadrer avec la Webfacto avant ouverture publique de la route `/t/*` (Phase 3) : politique de rétention, lifecycle rules S3, CDN.
